import type { MissionState } from '../mission/types.js'
import { verificationSatisfied } from '../verification/policy.js'
import { syncMissionGates } from '../gates/gates.js'
import { missionRequiresPackagePublish, missionRequiresReleaseCreate } from '../safety/release-chain.js'
export interface CompletionCheck{complete:boolean;reasons:string[];next?:'VERIFY'|'RECONCILE'|'USER_ACTION_REQUIRED'|'CONTINUE'}
export function evaluateCompletion(m:MissionState):CompletionCheck{const reasons:string[]=[];syncMissionGates(m);if(missionRequiresReleaseCreate(m.objective)){if(m.release_chain?.release?.outcome!=='success')reasons.push('release-chain:release-not-completed');else if(!m.release_chain?.release?.remote_verified)reasons.push('release-chain:release-remote-unverified');}if(missionRequiresPackagePublish(m.objective)){if(m.release_chain?.package?.outcome!=='success')reasons.push('release-chain:package-not-published');else if(!m.release_chain?.package?.remote_verified)reasons.push('release-chain:package-remote-unverified');}if(m.user_interrupted||m.status==='stopped')return{complete:false,reasons:['user-stopped']};if(m.workers.some(w=>['created','queued','starting','busy'].includes(w.status)))reasons.push('active-worker');if(m.tasks.some(t=>['created','queued','running','waiting'].includes(t.status)))reasons.push('pending-task');if(m.native_todos_incomplete>0)reasons.push(`native-todos-incomplete:${m.native_todos_incomplete}`);if(m.blockers.length)reasons.push(`blockers:${m.blockers.slice(0,6).join('|')}`);const open=m.obligations.filter(o=>o.status==='open');if(open.length)reasons.push(`open-obligations:${open.map(o=>o.id).join(',')}`);const authorityGate=m.gates.find(g=>g.kind==='user-authority'&&g.status!=='closed');if(authorityGate)return{complete:false,reasons:[...reasons,`authority:${authorityGate.status}`],next:'USER_ACTION_REQUIRED'};const rollback=m.gates.find(g=>g.kind==='rollback'&&g.status!=='closed');if(rollback)return{complete:false,reasons:[...reasons,'temporary-rollback-pending'],next:'USER_ACTION_REQUIRED'};const prereq=m.gates.find(g=>g.kind==='prerequisite-task'&&g.status!=='closed');if(prereq)reasons.push('prerequisite-task-pending');const verificationRequired=m.obligations.some(o=>o.kind==='verification');if(verificationRequired){const check=verificationSatisfied(m);if(!check.ok)return{complete:false,reasons:[...reasons,`verification-missing:${check.missing.join(',')}`],next:'VERIFY'}}if(m.tasks.some(t=>t.result?.status==='FIX_REQUIRED'||t.result?.status==='NEEDS_CONTEXT'))return{complete:false,reasons:[...reasons,'worker-result-unreconciled'],next:'RECONCILE'};return reasons.length?{complete:false,reasons,next:'CONTINUE'}:{complete:true,reasons:['all-required-obligations-closed','no-pending-work','fresh-evidence','all-gates-closed']}}
