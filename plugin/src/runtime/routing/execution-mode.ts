import type { ExecutionMode,MissionState,NormalizedMissionIntent } from '../mission/types.js'
export interface ExecutionModeDecision{mode:ExecutionMode;reason:string[]}
export function resolveExecutionMode(intent:NormalizedMissionIntent,m?:MissionState):ExecutionModeDecision{if(m?.execution.execution_mode==='team')return{mode:'parallel',reason:['legacy team execution mode normalized to scheduler-owned parallel topology']};if(intent.risk==='authority-boundary')return{mode:'single',reason:['authority boundary forbids speculative parallel work']};if(intent.scope==='multi-stream')return{mode:'parallel',reason:['structured multi-stream scope proves independent workstreams']};if(intent.scope==='local'||intent.requiredCapabilities.length<=1)return{mode:'single',reason:['minimum sufficient execution']};const independentReview=intent.taskKind==='review'&&['security-review','visual-qa','review'].filter(x=>intent.requiredCapabilities.includes(x)).length>=2;if(independentReview)return{mode:'parallel',reason:['multiple independent read-only review domains are proven']};return{mode:'single',reason:['no proven independent parallel branches']}}
