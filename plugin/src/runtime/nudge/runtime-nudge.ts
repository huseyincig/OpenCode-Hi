import type { MissionState, RuntimeNudge } from '../mission/types.js'
export function setRuntimeNudge(m:MissionState,instruction:string,reason:string,task_id?:string,worker_id?:string):RuntimeNudge{const n={id:`n_${Date.now().toString(36)}`,reason,instruction,created_at:Date.now(),generation:m.continuation.generation,task_id,worker_id};m.continuation.pending_nudge=n;return n}
export function consumeRuntimeNudge(m:MissionState):RuntimeNudge|undefined{const n=m.continuation.pending_nudge;if(n&&n.generation===m.continuation.generation)m.continuation.pending_nudge=undefined;return n?.generation===m.continuation.generation?n:undefined}
