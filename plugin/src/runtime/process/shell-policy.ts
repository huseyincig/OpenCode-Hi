export type ShellDecision='ALLOW'|'REWRITE'|'USER_ACTION_REQUIRED'|'DENY'
export interface ShellPolicyResult{decision:ShellDecision;command:string;reason:string}
const INTERACTIVE=[/\bssh\b(?!.*\s-[^\n]*T)/i,/\bpasswd\b/i,/\b(?:npm|pnpm|yarn)\s+login\b/i,/\bgh\s+auth\s+login\b/i,/\b(?:az|gcloud|aws)\s+login\b/i,/\bselect\s+/i]
export function evaluateShellCommand(command:string):ShellPolicyResult{const c=command.trim();if(!c)return{decision:'DENY',command:c,reason:'empty command'};if(/^\s*yes\s*\|/i.test(c)||/\|\s*yes\s*$/i.test(c))return{decision:'DENY',command:c,reason:'blanket approval bypass is forbidden'};if(/\bnpm\s+init\b(?!.*\s-y\b)/i.test(c))return{decision:'REWRITE',command:c.replace(/\bnpm\s+init\b/i,'npm init -y'),reason:'known safe non-interactive form'};if(INTERACTIVE.some(r=>r.test(c)))return{decision:'USER_ACTION_REQUIRED',command:c,reason:'interactive credential or terminal flow requires real user interaction'};return{decision:'ALLOW',command:c,reason:'non-interactive command'} }
