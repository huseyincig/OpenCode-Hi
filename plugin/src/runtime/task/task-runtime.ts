import { createHash } from 'node:crypto'
import type { HiConfig } from '../../config/schema.js'
import type { Category,ExecutionProfile,MissionState,WorkerResult,WorkerState } from '../mission/types.js'
import { resolveCategory,roleForIntent } from '../routing/category.js'
import { resolveModel,runtimeModelCandidateStatus,type AvailableModel } from '../routing/model-resolver.js'
import { configuredSkillPaths,discoverSkills,resolveSkillPlan } from '../skills/registry.js'
import { resolveSkillPermissionMap,resolveSkillToolEnabled } from '../skills/permissions.js'
import { createTask,createWorker,applyWorkerResult,workerFingerprint } from '../worker/worker-runtime.js'
import { BackgroundRegistry } from '../background/registry.js'
import { workerHandoffText } from './contracts.js'
import { createChildSession,sendPromptAsync,abortSession } from '../../opencode/client-adapter.js'
import { NativeOpenCodeAdapter } from '../../opencode/native-adapter.js'
import { appendLedger } from '../ledger/ledger.js'
import { ConcurrencyScheduler } from '../scheduler/concurrency.js'
import { addEvidence,markMutation } from '../evidence/evidence-runtime.js'
import { parallelSafety } from '../scheduler/parallel-safety.js'
import { routeCapabilities } from '../routing/capability-router.js'
import { replanVerificationForChangedSurface,verificationEconomyInstruction,verificationSatisfied } from '../verification/policy.js'
import { targetedVerificationHint } from '../verification/discovery.js'
import { collectRepoContext } from '../intent/repo-context.js'
import { buildMethodologyBundle,methodologyProvenance,ownershipContract } from '../skills/methodology.js'
import { DEFAULT_CONTEXT_BUDGET,clipList,clipText } from '../context/budget.js'
import { modelQuirks } from '../routing/model-quirks.js'
import type { RuntimeSignalSink } from '../events/event-sink.js'
import { runtimeSignal } from '../events/event-sink.js'
import { syncMissionGates } from '../gates/gates.js'
import { classifyWorkerFailure } from '../worker/failure-classifier.js'
import { assessDiffOwnership } from './diff-ownership.js'
import { recordPreexistingUserBaseline } from '../safety/staging-safety.js'
import { evaluateTaskPreconditions,TaskPreconditionError } from '../readiness/preconditions.js'
import { effectiveExecutionSurface,promptToolOverrides } from '../routing/execution-profile.js'
import { redactProviderContext } from '../privacy/boundary.js'

export interface StartTaskInput{objective?:string;role?:string;category?:Category;scope?:string[];dependencies?:string[];requiredEvidence?:string[];obligationIds?:string[];model?:string;modelVariant?:string;relevantContext?:string[];constraints?:string[];forkFromSession?:string}
const CATEGORIES=new Set(['quick','standard','deep','visual','critical']),CHILD_ROLES=new Set(['coder','architect','repository-explorer','qa-reviewer','security-reviewer','visual-qa']),READ_ONLY=new Set(['architect','repository-explorer','qa-reviewer','security-reviewer','visual-qa'])
const MAX_QUEUE=32
function missionModelFeedback(m:MissionState){
  const failures:Record<string,number>={},successes:Record<string,number>={},retries:Record<string,number>={}
  const inc=(r:Record<string,number>,id?:string,n=1)=>{if(id)r[id]=(r[id]??0)+n}
  for(const w of m.workers){
    const observed=w.effective_model??w.model
    if(w.status==='completed')inc(successes,observed)
    if(w.status==='failed')inc(failures,observed)
    if(w.last_runtime_failure_kind&&w.model)inc(failures,w.model)
    for(const h of w.fallback_history??[]){inc(retries,h.from);if(/failure=|provider|transport|tool|context/i.test(h.reason))inc(failures,h.from)}
  }
  return{failures,successes,retries}
}

function words(text:string):Set<string>{return new Set(text.toLowerCase().replace(/[^a-z0-9]+/gi,' ').split(/\s+/).filter(x=>x.length>2))}
function overlap(a:string,b:string):number{const aa=words(a),bb=words(b);let n=0;for(const x of aa)if(bb.has(x))n++;return n}
function inferObligationIds(m:MissionState,role:string,objective:string,requiredEvidence:string[],explicit:string[]=[]):string[]{
  const validExplicit=[...new Set(explicit)].filter(id=>m.obligations.some(o=>o.id===id&&o.status==='open'));if(validExplicit.length)return validExplicit
  const kinds:string[]=[];if(role==='coder')kinds.push('implementation');if(['repository-explorer','architect'].includes(role)||role==='coder'&&m.intent.taskKind==='bug-fix')kinds.push('analysis');if(READ_ONLY.has(role))kinds.push('review');if(requiredEvidence.length)kinds.push('verification')
  const out:string[]=[]
  for(const kind of [...new Set(kinds)]){const candidates=m.obligations.filter(o=>o.kind===kind&&o.status==='open');if(candidates.length===1){out.push(candidates[0].id);continue}if(candidates.length>1){const ranked=candidates.map(o=>({o,score:overlap(objective,o.summary)})).sort((a,b)=>b.score-a.score);if(ranked[0].score>0&&ranked[0].score>ranked[1].score)out.push(ranked[0].o.id)}}
  return [...new Set(out)]
}
function resultDigest(result:WorkerResult):string{return createHash('sha256').update(JSON.stringify(result)).digest('hex')}
function providerOf(model:string|undefined):string|undefined{return model&&model!=='host-default'&&model.includes('/')?model.slice(0,model.indexOf('/')):undefined}
function normFile(value:string):string{return value.trim().replace(/\\/g,'/').replace(/^\.\//,'')}
function nativeDiffMap(raw:any):Record<string,string>{
  const items=Array.isArray(raw)?raw:Array.isArray(raw?.data)?raw.data:[];const out:Record<string,string>={}
  for(const item of items){const file=typeof item?.file==='string'?normFile(item.file):'';if(!file)continue;const signature=createHash('sha256').update(JSON.stringify({file,additions:item?.additions??null,deletions:item?.deletions??null,status:item?.status??null,before:item?.before??null,after:item?.after??null,patch:item?.patch??null})).digest('hex');out[file]=signature}
  return out
}
function diffDelta(before:Record<string,string>|undefined,after:Record<string,string>):string[]{const b=before??{};return Object.keys(after).filter(file=>b[file]!==after[file])}
interface QueueEntry{mission:MissionState;worker:WorkerState;run:()=>Promise<WorkerState>;created:number}
export class TaskRuntime{
  #queue:QueueEntry[]=[];#draining=false
  constructor(private client:any,private registry:BackgroundRegistry,private scheduler:ConcurrencyScheduler,private projectRoot:string,private hiRoot:string,private getConfig:()=>HiConfig,private getModels:()=>AvailableModel[],private getHostConfig:()=>Record<string,unknown>,private events?:RuntimeSignalSink){}
  private async sendProviderPrompt(sessionID:string,text:string,role?:string,model?:string,variant?:string,tools?:Record<string,boolean>):Promise<unknown>{const safe=redactProviderContext(text);return sendPromptAsync(this.client,sessionID,safe.providerText,role,model,variant,tools)}
  private async captureNativeDiff(worker:WorkerState,phase:'baseline'|'final'):Promise<Record<string,string>|undefined>{
    if(!worker.session_id)return undefined;const native=new NativeOpenCodeAdapter(this.client);if(!native.has('diff'))return undefined
    try{const map=nativeDiffMap(await native.diff(worker.session_id));if(phase==='baseline')worker.native_diff_baseline=map;else worker.native_diff_final=map;return map}catch{return undefined}
  }
  async reconcileNativeResult(m:MissionState,workerID:string,result:WorkerResult):Promise<WorkerResult>{
    const worker=m.workers.find(w=>w.id===workerID);if(!worker)return result;const task=m.tasks.find(t=>t.id===worker.task_id);if(!task)return result
    const final=await this.captureNativeDiff(worker,'final');const baseline=worker.native_diff_baseline??{};const nativeDelta=final?diffDelta(baseline,final):[];const reportedRaw=[...new Set((result.changed_files??[]).map(normFile).filter(Boolean))];const observedRaw=[...new Set((worker.write_set??[]).map(normFile).filter(Boolean))]
    // Session diff can contain user-owned dirty files that existed before this worker started.
    // A file whose native diff signature is identical to the worker baseline is not part of the
    // worker's net delta, even if the worker self-reports it or briefly touched and restored it.
    // This prevents Hi cleanup from stealing/reverting pre-existing user work.
    const preservedPreexisting=final?reportedRaw.filter(file=>baseline[file]!==undefined&&final[file]===baseline[file]):[]
    const reported=reportedRaw.filter(file=>!preservedPreexisting.includes(file));const observed=final?observedRaw.filter(file=>!(baseline[file]!==undefined&&final[file]===baseline[file])):observedRaw
    if(preservedPreexisting.length)appendLedger(m,'user-diff.preserved',{task_id:task.id,worker_id:worker.id,payload:{files:preservedPreexisting.slice(0,40),policy:'baseline-owned-by-user'}})
    const previousCollateral=[...new Set((task.diff_cleanliness?.collateral??[]).map(normFile).filter(Boolean))]
    if(previousCollateral.length){
      if(!final){
        const marker=`cleanup-unverified:${task.id}:${previousCollateral.slice(0,12).sort().join(',')}`
        appendLedger(m,'diff.cleanup.unverified',{task_id:task.id,worker_id:worker.id,payload:{files:previousCollateral.slice(0,40),reason:'native-diff-unavailable'}})
        return {...result,status:'FIX_REQUIRED',summary:`Cleanup cannot be accepted without native diff evidence. Verify that collateral files are restored before completion: ${previousCollateral.slice(0,12).join(', ')}.`,open_issues:[...new Set([...(result.open_issues??[]),marker])],needs_context:[...new Set([...(result.needs_context??[]),'cleanup-verification: native/session diff evidence is unavailable; do not claim collateral revert as complete until current diff can be deterministically verified'])]}
      }
      const stillDirty=previousCollateral.filter(file=>baseline[file]!==final[file])
      if(stillDirty.length){
        const marker=`cleanup-not-reverted:${task.id}:${stillDirty.slice(0,12).sort().join(',')}`
        appendLedger(m,'diff.cleanup.failed',{task_id:task.id,worker_id:worker.id,payload:{files:stillDirty.slice(0,40)}})
        return {...result,status:'FIX_REQUIRED',summary:`Worker reported cleanup, but native diff still shows collateral changes: ${stillDirty.slice(0,12).join(', ')}.`,changed_files:[...new Set([...reported,...stillDirty])],open_issues:[...new Set([...(result.open_issues??[]),marker])],needs_context:[...new Set([...(result.needs_context??[]),'cleanup-verification: inspect native/session diff and actually revert remaining collateral changes before reporting DONE'])]}
      }
      task.diff_cleanliness={collateral:[...(task.diff_cleanliness?.collateral??[])],accepted_expansions:[...(task.diff_cleanliness?.accepted_expansions??[])],native_verified_reverts:[...previousCollateral]};appendLedger(m,'diff.cleanup.verified',{task_id:task.id,worker_id:worker.id,payload:{reverted:previousCollateral.slice(0,40),source:'native-session-diff-baseline'}})
    }
    const activeWriters=m.workers.filter(w=>!READ_ONLY.has(w.role)&&['starting','busy'].includes(w.status));const soleWriter=activeWriters.length<=1||activeWriters.every(w=>w.id===worker.id)
    const attributedNative=nativeDelta.filter(file=>observed.includes(file)||task.scope.map(normFile).includes(file)||(soleWriter&&!reported.includes(file)))
    const actual=[...new Set([...observed,...attributedNative])];const missing=actual.filter(file=>!reported.includes(file))
    if(!missing.length){if(final)appendLedger(m,'native.diff.reconciled',{task_id:task.id,worker_id:worker.id,payload:{reported:reported.length,observed:observed.length,native_delta:nativeDelta.length,sole_writer:soleWriter}});return {...result,changed_files:reported}}
    const marker=`native-diff-mismatch:${task.id}:${missing.slice(0,12).sort().join(',')}`;appendLedger(m,'native.diff.mismatch',{task_id:task.id,worker_id:worker.id,payload:{missing:missing.slice(0,40),reported:reported.slice(0,40),observed:observed.slice(0,40),native_delta:nativeDelta.slice(0,40),sole_writer:soleWriter}})
    return {...result,status:'FIX_REQUIRED',summary:`Native/session write evidence disagrees with WorkerResult changed_files. Reconcile before completion: ${missing.slice(0,12).join(', ')}.`,changed_files:[...new Set([...reported,...actual])],open_issues:[...new Set([...(result.open_issues??[]),marker])],needs_context:[...new Set([...(result.needs_context??[]),'native-diff-reconcile: inspect the current native/session diff and return a complete changed_files list; do not conceal or silently discard writes'])]}
  }
  noteEffectiveModel(m:MissionState,workerID:string,observed?:{model?:string;variant?:string;source?:string}):{ok:boolean;expected?:string;observed?:string;reason:string}{
    const worker=m.workers.find(w=>w.id===workerID);if(!worker)return{ok:false,reason:'worker-not-found'}
    const task=m.tasks.find(t=>t.id===worker.task_id),expected=worker.model,expectedVariant=worker.model_variant,taskID=task?.id??worker.task_id
    const clearModelMarkers=()=>{m.blockers=m.blockers.filter(b=>!b.startsWith(`model-effective-unverified:${taskID}:`)&&!b.startsWith(`model-effective-mismatch:${taskID}:`)&&!b.startsWith(`model-variant-unverified:${taskID}:`)&&!b.startsWith(`model-variant-mismatch:${taskID}:`))}
    worker.effective_model=observed?.model;worker.effective_model_variant=observed?.variant;worker.effective_model_source=observed?.source??'assistant-message-metadata';worker.effective_model_observed_at=Date.now();worker.effective_model_variant_verified=expectedVariant?false:undefined
    if(!expected||expected==='host-default'){worker.effective_model_verified=Boolean(observed?.model);appendLedger(m,'model.effective.observed',{task_id:task?.id,worker_id:worker.id,payload:{expected:expected??'host-default',observed:observed?.model??'host-default/unreported',expected_variant:expectedVariant,variant:observed?.variant,source:worker.effective_model_source}});return{ok:true,expected,observed:observed?.model,reason:'host-default-or-unconstrained'}}
    if(!observed?.model){worker.effective_model_verified=false;const marker=`model-effective-unverified:${taskID}:${expected}`;if(!m.blockers.includes(marker))m.blockers.push(marker);appendLedger(m,'model.effective.unverified',{task_id:task?.id,worker_id:worker.id,payload:{expected,expected_variant:expectedVariant,source:worker.effective_model_source}});return{ok:false,expected,reason:marker}}
    if(observed.model!==expected){worker.effective_model_verified=false;const marker=`model-effective-mismatch:${taskID}:${expected}->${observed.model}`;clearModelMarkers();m.blockers.push(marker);appendLedger(m,'model.effective.mismatch',{task_id:task?.id,worker_id:worker.id,payload:{expected,observed:observed.model,expected_variant:expectedVariant,variant:observed.variant,source:worker.effective_model_source}});return{ok:false,expected,observed:observed.model,reason:marker}}
    worker.effective_model_verified=true
    if(expectedVariant){
      if(!observed?.variant){const marker=`model-variant-unverified:${taskID}:${expectedVariant}`;clearModelMarkers();m.blockers.push(marker);appendLedger(m,'model.variant.unverified',{task_id:task?.id,worker_id:worker.id,payload:{model:expected,expected_variant:expectedVariant,source:worker.effective_model_source}});return{ok:false,expected,observed:observed.model,reason:marker}}
      if(observed.variant!==expectedVariant){const marker=`model-variant-mismatch:${taskID}:${expectedVariant}->${observed.variant}`;clearModelMarkers();m.blockers.push(marker);appendLedger(m,'model.variant.mismatch',{task_id:task?.id,worker_id:worker.id,payload:{model:expected,expected_variant:expectedVariant,observed_variant:observed.variant,source:worker.effective_model_source}});return{ok:false,expected,observed:observed.model,reason:marker}}
      worker.effective_model_variant_verified=true
    }
    clearModelMarkers();appendLedger(m,'model.effective.verified',{task_id:task?.id,worker_id:worker.id,payload:{expected,observed:observed.model,expected_variant:expectedVariant,variant:observed.variant,variant_verified:expectedVariant?true:undefined,source:worker.effective_model_source}});return{ok:true,expected,observed:observed.model,reason:expectedVariant?'effective-model-and-variant-match-runtime-selection':'effective-model-matches-runtime-selection'}
  }
  queueDepth():number{return this.#queue.length}
  private depsReady(m:MissionState,deps:string[]):boolean{return deps.every(id=>m.tasks.find(t=>t.id===id)?.status==='completed')}
  private failedDeps(m:MissionState,deps:string[]):string[]{return deps.filter(id=>{const status=m.tasks.find(t=>t.id===id)?.status;return status==='failed'||status==='cancelled'})}
  private canRun(m:MissionState,worker:WorkerState,chain:string[]):boolean{if(m.status!=='active'||m.user_interrupted||worker.status==='cancelled')return false;if(!this.depsReady(m,m.tasks.find(t=>t.id===worker.task_id)?.dependencies??[]))return false;if(m.execution_mode==='single'&&m.workers.some(w=>w.id!==worker.id&&['starting','busy'].includes(w.status)))return false;return chain.some(model=>this.scheduler.canStart(worker.id,providerOf(model),model==='host-default'?undefined:model).ok)}
  private queueTask(m:MissionState,worker:WorkerState,run:()=>Promise<WorkerState>):void{if(this.#queue.length>=MAX_QUEUE)throw new Error('Hi bounded dispatch queue is full');const t=m.tasks.find(x=>x.id===worker.task_id);worker.status='queued';if(t)t.status='queued';if(!this.#queue.some(x=>x.worker.id===worker.id))this.#queue.push({mission:m,worker,run,created:Date.now()});this.registry.set(worker);appendLedger(m,'worker.queued',{task_id:t?.id,worker_id:worker.id,payload:{queue_depth:this.#queue.length}});void this.events?.(runtimeSignal('worker.queued',m.mission_id,{task_id:t?.id,worker_id:worker.id,payload:{queue_depth:this.#queue.length}}));syncMissionGates(m)}
  private drainQueue():void{if(this.#draining)return;this.#draining=true;queueMicrotask(async()=>{try{let progress=true;while(progress){progress=false;for(let i=0;i<this.#queue.length;i++){const e=this.#queue[i],t=e.mission.tasks.find(x=>x.id===e.worker.task_id),chain=[e.worker.model,...e.worker.fallbacks].filter((x):x is string=>Boolean(x));if(e.mission.status!=='active'||e.mission.user_interrupted||e.worker.status==='cancelled'){this.#queue.splice(i--,1);continue}if(!t){this.#queue.splice(i--,1);continue}const failed=this.failedDeps(e.mission,t.dependencies);if(failed.length){this.#queue.splice(i--,1);e.worker.status='failed';t.status='blocked';t.updated_at=Date.now();const reason=`dependency-unavailable:${failed.join(',')}`;t.result={status:'BLOCKED',summary:'Required dependency did not complete successfully.',changed_files:[],evidence:[],open_issues:[reason],needs_context:[]};e.mission.blockers=[...new Set([...e.mission.blockers,reason])];this.registry.delete(e.worker.id);appendLedger(e.mission,'worker.dependency-blocked',{task_id:t.id,worker_id:e.worker.id,payload:{dependencies:failed}});void this.events?.(runtimeSignal('worker.dependency-blocked',e.mission.mission_id,{task_id:t.id,worker_id:e.worker.id,payload:{dependencies:failed}}));syncMissionGates(e.mission);progress=true;continue}if(!this.depsReady(e.mission,t.dependencies)||!this.canRun(e.mission,e.worker,chain))continue;this.#queue.splice(i--,1);progress=true;try{await e.run()}catch{}}} }finally{this.#draining=false}})}
  async start(m:MissionState,input:StartTaskInput={}):Promise<{task_id:string;worker_id:string;session_id?:string;model?:string;skills:string[];selection_reason:string[];readiness:'READY'|'WAIT';preconditions:Array<{id:string;decision:string;reason:string}>}>{
    if(m.status!=='active'||m.user_interrupted)throw new Error('Mission is not active')
    const objective=input.objective?.trim()||m.objective,routed=routeCapabilities(m.intent),defaultCategory=resolveCategory(m.intent),category=(CATEGORIES.has(String(input.category))?input.category:(routed.category??defaultCategory)) as Category,routedRole=CHILD_ROLES.has(routed.role)?routed.role:roleForIntent(m.intent),defaultRole=CHILD_ROLES.has(routedRole)?routedRole:'coder',role=CHILD_ROLES.has(String(input.role))?String(input.role):defaultRole
    const hostConfig=this.getHostConfig(),feedback=missionModelFeedback(m),selected=resolveModel(category,this.getModels(),this.getConfig(),input.model,role,hostConfig,feedback);if(selected.rejected.length)appendLedger(m,'model.policy.rejected',{payload:{items:selected.rejected.slice(0,20)}});if(selected.scores?.length)appendLedger(m,'model.scored',{payload:{role,category,top:selected.scores.slice(0,6),feedback}})
    const candidates=discoverSkills(this.projectRoot,this.hiRoot,configuredSkillPaths(hostConfig)),permissionMap=resolveSkillPermissionMap(hostConfig,role),skillToolEnabled=resolveSkillToolEnabled(hostConfig,role),surface=effectiveExecutionSurface(hostConfig,role,skillToolEnabled),skillPlan=resolveSkillPlan(m.intent.requiredCapabilities,candidates,permissionMap,skillToolEnabled,role),skills=skillPlan.selected.map(s=>s.name)
    appendLedger(m,'skill.resolved',{payload:{role,requested:skillPlan.requested,outcomes:skillPlan.outcomes}});void this.events?.(runtimeSignal('skill.resolved',m.mission_id,{payload:{role,requested:skillPlan.requested,outcomes:skillPlan.outcomes}}));if(skillPlan.missing.length)appendLedger(m,'skill.fallback',{payload:{missing:skillPlan.missing,requested:skillPlan.requested,skillToolEnabled}})
    const scope=input.scope??(READ_ONLY.has(role)&&m.changed_files.length?m.changed_files:m.intent.likelyTargets??[]),dependencies=[...new Set(input.dependencies??[])];const unknownDependencies=dependencies.filter(id=>!m.tasks.some(t=>t.id===id)),unavailableDependencies=this.failedDeps(m,dependencies),incompleteDependencies=dependencies.filter(id=>{const t=m.tasks.find(x=>x.id===id);return Boolean(t)&&t!.status!=='completed'&&!unavailableDependencies.includes(id)})
    const requiredEvidence=input.requiredEvidence??m.verification_policy.requiredKinds,obligationIds=inferObligationIds(m,role,objective,requiredEvidence,input.obligationIds),constraints=[...new Set([...(m.constraints??[]),...(input.constraints??[])])],desiredFingerprint=workerFingerprint(role,category,selected.primary,m.intent.taskKind,objective,{scope,constraints,dependencies,requiredEvidence,obligationIds}),existing=m.workers.find(w=>w.fingerprint===desiredFingerprint&&!['completed','failed','cancelled'].includes(w.status))
    const native=new NativeOpenCodeAdapter(this.client),resumeCapable=Boolean(existing?.session_id),preflight=evaluateTaskPreconditions({role,implementation:role==='coder',dependencies:{unknown:unknownDependencies,failed:unavailableDependencies,incomplete:incompleteDependencies},modelAvailable:Boolean(selected.primary),native:{childSession:resumeCapable||native.has('session-create'),prompt:native.has('prompt-async')||native.has('prompt-sync')},hostConfig,contractCriticalAmbiguity:m.intent.ambiguity==='contract-critical',authorityRequired:false});appendLedger(m,'task.preflight',{payload:{role,decision:preflight.decision,resume_capable:resumeCapable,items:preflight.items.slice(0,12)}});void this.events?.(runtimeSignal('task.preflight',m.mission_id,{payload:{role,decision:preflight.decision,resume_capable:resumeCapable,items:preflight.items.slice(0,12)}}));if(preflight.decision==='RESOLVE'||preflight.decision==='USER_ACTION_REQUIRED')throw new TaskPreconditionError(preflight)
    if(existing){const oldTask=m.tasks.find(t=>t.id===existing.task_id);if(existing.status==='ready'&&existing.session_id&&oldTask?.result&&['FIX_REQUIRED','NEEDS_CONTEXT','BLOCKED'].includes(oldTask.result.status)){const chain=[selected.primary,...selected.fallbacks].filter((x):x is string=>Boolean(x)),nextModel=chain.find(model=>this.scheduler.canStart(existing.id,providerOf(model),model==='host-default'?undefined:model).ok)??existing.model;if(!nextModel)throw new Error('Worker resume capacity unavailable');const previousModel=existing.model;this.scheduler.acquire(existing.id,providerOf(nextModel),nextModel==='host-default'?undefined:nextModel);existing.model=nextModel;existing.generation_at_spawn=m.generation;existing.status='busy';existing.started_at=Date.now();oldTask.status='running';this.registry.set(existing);const issues=oldTask.result.open_issues.join(' | '),missing=oldTask.result.needs_context.join(' | '),reviewScope=READ_ONLY.has(existing.role)?`Scoped rereview only: previous findings=${issues||'none'}; changed scope=${m.changed_files.slice(-20).join(',')||'none'}; affected evidence=${m.evidence.items.filter(e=>!e.invalidated_at).slice(-8).map(e=>e.summary).join(' | ')||'none'}.`:'';const resumeVariant=nextModel===selected.primary?selected.primaryVariant:selected.fallbackVariants[nextModel];const protectedBaseline=Object.keys(existing.native_diff_baseline??{}).slice(0,60);await this.sendProviderPrompt(existing.session_id,clipText([`Hi corrective resume for existing task ${oldTask.id}.`,`Previous status: ${oldTask.result.status}.`,`Missing context: ${missing||'none'}.`,`Open issues: ${issues||'none'}.`,`Current user constraints: ${(oldTask.constraints??[]).join(' | ')||'none'}.`,protectedBaseline.length?`PRE-EXISTING USER DIRTY BASELINE: ${protectedBaseline.join(', ')}. Cleanup means restore these paths to their exact worker-start baseline, NOT to HEAD. Never discard user-owned edits with git checkout/reset/restore.`:'Pre-existing user dirty baseline: none observed.',reviewScope,'Resume from current session context. Apply the smallest correction. Do not restart planning or create sub-orchestrators. Return the structured WorkerResult again.'].filter(Boolean).join('\n'),DEFAULT_CONTEXT_BUDGET.max_handoff_chars),existing.role,nextModel==='host-default'?undefined:nextModel,resumeVariant,promptToolOverrides(oldTask.execution_profile?.tools??[]));existing.model_variant=resumeVariant;existing.restart_reconcile_pending=false;appendLedger(m,nextModel!==previousModel?'worker.model-escalated':'worker.resumed',{task_id:oldTask.id,worker_id:existing.id,payload:{status:oldTask.result.status,model:nextModel}})}return{task_id:oldTask?.id??existing.task_id,worker_id:existing.id,session_id:existing.session_id,model:existing.model,skills:existing.loaded_skills,selection_reason:['same-session worker reuse'],readiness:'READY',preconditions:preflight.items}}
    const safety=parallelSafety(m.tasks,{scope,dependencies,role}),hardParallelConflicts=safety.reasons.filter(reason=>!reason.startsWith('dependency:'));if(hardParallelConflicts.length&&m.tasks.some(t=>['queued','running'].includes(t.status)))throw new Error(`Unsafe parallel dispatch: ${hardParallelConflicts.join('; ')}`)
    const approvalGated=skillPlan.selected.filter(s=>s.permission==='ask').map(s=>s.name),taskTools=surface.tools.filter(t=>t!=='skill'||approvalGated.length>0)
    const profile:ExecutionProfile={role,category,task:{objective,scope:[...scope],dependencies:[...dependencies],required_evidence:[...requiredEvidence]},tools:taskTools,model:selected.primary,model_variant:input.modelVariant??selected.primaryVariant,fallback_models:selected.fallbacks,fallback_variants:selected.fallbackVariants,fallback_reasons:selected.fallbackReasons,skills,permission_profile:{skill_tool_enabled:skillToolEnabled,skill_permissions:permissionMap??{},external_effects:'parent-only',recursive_task:'deny',native:surface.permissions},verification_policy:{...m.verification_policy,requiredKinds:[...m.verification_policy.requiredKinds]},max_context_chars:DEFAULT_CONTEXT_BUDGET.max_context_chars,max_handoff_chars:DEFAULT_CONTEXT_BUDGET.max_handoff_chars,max_result_chars:DEFAULT_CONTEXT_BUDGET.max_result_chars,max_artifacts:DEFAULT_CONTEXT_BUDGET.max_artifacts}
    const task=createTask(m,{objective,role,category,scope,constraints,dependencies,requiredEvidence,obligationIds,contextArtifacts:m.context_artifacts.slice(-DEFAULT_CONTEXT_BUDGET.max_artifacts),executionProfile:profile}),provenance=methodologyProvenance(skillPlan.selected),worker=createWorker(m,task,selected.primary,selected.fallbacks,skills,provenance);worker.model_selection_reason=[...selected.reason];worker.fallback_history=[]
    const autoLoad=skillPlan.selected.filter(s=>s.permission!=='ask'),methodology=buildMethodologyBundle(autoLoad),quirks=modelQuirks(selected.primary);if(methodology.truncated.length)appendLedger(m,'skill.payload.truncated',{task_id:task.id,worker_id:worker.id,payload:{skills:methodology.truncated}})
    const artifactContext=task.context_artifacts.map(a=>`${a.kind}:${a.title??a.id}${a.summary?` — ${a.summary}`:''}`),verificationHint=targetedVerificationHint(this.projectRoot,task.scope.length?task.scope:(m.changed_files.length?m.changed_files:m.intent.likelyTargets??[])),explicitRelevant=input.relevantContext??[],boundedRuntimeRelevant=[...(verificationHint?[verificationHint]:[]),...artifactContext];let nativeSummary:string|undefined,relevantForHandoff=[...explicitRelevant,...boundedRuntimeRelevant]
    if(relevantForHandoff.join('\n').length>profile.max_context_chars){const native=new NativeOpenCodeAdapter(this.client);if(native.has('summarize'))try{const summary=await native.summarize(m.session_id);nativeSummary=clipText(typeof summary==='string'?summary:JSON.stringify(summary),Math.min(6000,Math.floor(profile.max_context_chars/2)));relevantForHandoff=[`native-session-summary:${nativeSummary}`,...boundedRuntimeRelevant];appendLedger(m,'context.native-summary-used',{task_id:task.id,payload:{source_session:m.session_id,replaced_explicit_context:true}})}catch(error){appendLedger(m,'context.native-summary-unavailable',{task_id:task.id,payload:{error:String(error)}})};if(!nativeSummary)relevantForHandoff=clipList(relevantForHandoff,profile.max_context_chars)}
    const buildHandoff=()=>{const preexisting=Object.keys(worker.native_diff_baseline??{}).slice(0,60),core=workerHandoffText({objective,scope:task.scope,constraints:clipList([...(task.constraints??[]),'minimum sufficient change','no unrequested publish/push/deploy','return compact evidence',preexisting.length?`pre-existing user dirty paths at worker start: ${preexisting.join(', ')}; preserve their exact baseline state unless the task explicitly requires changing them; never use git checkout/reset/restore in a way that discards user-owned edits`:'no pre-existing native dirty paths were observed at worker start',verificationEconomyInstruction(m),`model-quirks:${JSON.stringify(quirks)}`],5000),required_evidence:task.requiredEvidence,relevant_context:clipList(relevantForHandoff,profile.max_context_chars),skills,approval_gated_skills:approvalGated,expected_output:{status:true,summary:true,changed_files:true,scope_expansions:true,evidence:true,open_issues:true}},profile.max_handoff_chars),full=[ownershipContract('child',skills),core,methodology.text].filter(Boolean).join('\n\n');return clipText(full,profile.max_handoff_chars)}
    const chain=[selected.primary,...selected.fallbacks].filter((x):x is string=>Boolean(x)),toolOverrides=promptToolOverrides(profile.tools)
    const run=()=>this.registry.dedupeSpawn(worker.fingerprint,async()=>{let lastError:unknown=new Error('No runtime model available');for(let i=0;i<chain.length;i++){if(m.status!=='active'||m.user_interrupted||worker.status==='cancelled'){worker.status='cancelled';task.status='cancelled';throw new Error('Mission stopped before worker dispatch')}const model=chain[i],variant=model===selected.primary?(input.modelVariant??selected.primaryVariant):selected.fallbackVariants[model],provider=providerOf(model),runtimeCandidate=runtimeModelCandidateStatus(model,this.getModels(),this.getConfig(),this.getHostConfig());if(!runtimeCandidate.ok){lastError=new Error(`Runtime model candidate rejected at dispatch: ${model}: ${runtimeCandidate.reason}`);appendLedger(m,'model.fallback.skipped',{task_id:task.id,worker_id:worker.id,payload:{model,reason:runtimeCandidate.reason,index:i,phase:'dispatch-revalidation'}});continue}const capacity=this.scheduler.canStart(worker.id,provider,model==='host-default'?undefined:model);if(!capacity.ok){lastError=new Error(`Worker capacity unavailable: ${capacity.reason}`);appendLedger(m,'model.fallback.skipped',{task_id:task.id,worker_id:worker.id,payload:{model,reason:capacity.reason,index:i}});continue}this.scheduler.acquire(worker.id,provider,model==='host-default'?undefined:model);worker.model=model;worker.model_variant=variant;try{worker.status='starting';task.status='queued';const native=new NativeOpenCodeAdapter(this.client);const canFork=false;const child=input.forkFromSession&&canFork&&native.has('fork')?await native.fork(input.forkFromSession,`Hi · ${role} · ${objective.slice(0,60)}`):await createChildSession(this.client,m.session_id,`Hi · ${role} · ${objective.slice(0,60)}`,role,model==='host-default'?undefined:model,variant);if(input.forkFromSession)appendLedger(m,'worker.session-fork',{task_id:task.id,worker_id:worker.id,payload:{source_session:input.forkFromSession,native:native.has('fork'),used:false,reason:'native fork cannot set specialist agent; created isolated child instead'}});if(m.status!=='active'||m.user_interrupted||(worker as WorkerState).status==='cancelled'){if(child?.id)try{await abortSession(this.client,child.id)}catch{};throw new Error('Mission stopped during worker spawn')}worker.session_id=child?.id;if(!worker.session_id)throw new Error('Child session id missing');recordPreexistingUserBaseline(m,await this.captureNativeDiff(worker,'baseline'));worker.generation_at_spawn=m.generation;worker.status='busy';worker.started_at=Date.now();task.status='running';this.registry.set(worker);if(i>0){const fallbackReason=selected.fallbackReasons[i-1]?.reason??`fallback-index:${i}`;worker.fallback_history=[...(worker.fallback_history??[]),{from:chain[i-1],to:model,variant,reason:fallbackReason,phase:'dispatch',at:Date.now()}]}void this.events?.(runtimeSignal('worker.started',m.mission_id,{task_id:task.id,worker_id:worker.id,payload:{model,variant,role}}));appendLedger(m,'worker.started',{task_id:task.id,worker_id:worker.id,payload:{session_id:worker.session_id,model,variant,index:i,reason:i===0?(input.modelVariant?[...selected.reason,'user-specified-variant']:selected.reason):[selected.fallbackReasons[i-1]?.reason??'runtime fallback',`fallback-index:${i}`]}});const handoff=buildHandoff();appendLedger(m,'worker.handoff',{task_id:task.id,worker_id:worker.id,payload:{chars:handoff.length,skills:worker.loaded_skills.length,tools:profile.tools.slice(0,20),permission_source:profile.permission_profile.native?.source,context_budget:profile.max_context_chars,handoff_budget:profile.max_handoff_chars,result_budget:profile.max_result_chars}});await this.sendProviderPrompt(worker.session_id,handoff,role,model==='host-default'?undefined:model,variant,toolOverrides);return worker}catch(error){lastError=error;this.scheduler.release(worker.id);if(worker.session_id){try{await abortSession(this.client,worker.session_id)}catch{};worker.session_id=undefined}if(m.status!=='active'||m.user_interrupted||(worker as WorkerState).status==='cancelled'){worker.status='cancelled';task.status='cancelled';appendLedger(m,'worker.start.cancelled',{task_id:task.id,worker_id:worker.id,payload:{model,index:i,error:String(error)}});throw error}appendLedger(m,'model.fallback.failed',{task_id:task.id,worker_id:worker.id,payload:{model,index:i,error:String(error)}});worker.status='created';task.status='created'}}worker.status='failed';const liveStatuses=chain.map(model=>({model,...runtimeModelCandidateStatus(model,this.getModels(),this.getConfig(),this.getHostConfig())}));const policyUnavailable=liveStatuses.length>0&&liveStatuses.every(x=>!x.ok);if(policyUnavailable){task.status='blocked';const marker=`model-dispatch-unavailable:${task.id}`;m.blockers=[...new Set([...m.blockers,marker])];task.result={status:'BLOCKED',summary:'No selected role model/fallback remains runtime-available and policy-permitted at dispatch time.',changed_files:[],evidence:[],open_issues:[marker],needs_context:['refresh provider/model inventory or routing/provider permissions']};appendLedger(m,'worker.start.model-unavailable',{task_id:task.id,worker_id:worker.id,payload:{attempted:liveStatuses}})}else task.status='failed';appendLedger(m,'worker.start.failed',{task_id:task.id,worker_id:worker.id,payload:{error:String(lastError),attempted_models:chain}});throw lastError})
    syncMissionGates(m);if(!this.canRun(m,worker,chain)){this.queueTask(m,worker,run);return{task_id:task.id,worker_id:worker.id,model:worker.model,skills:worker.loaded_skills,selection_reason:[...routed.reason,...selected.reason,...selected.fallbackReasons.map(x=>`${x.model}:${x.reason}`),'queued:runtime-capacity-or-prerequisite',...skillPlan.reason],readiness:'WAIT',preconditions:preflight.items}}
    const spawned=await run();if(spawned.id!==worker.id){const duplicateTask=m.tasks.find(t=>t.id===task.id);if(duplicateTask&&['created','queued'].includes(duplicateTask.status))m.tasks=m.tasks.filter(t=>t.id!==duplicateTask.id);m.workers=m.workers.filter(w=>w.id!==worker.id);appendLedger(m,'worker.spawn.deduped',{worker_id:spawned.id,payload:{discarded_worker_id:worker.id,fingerprint:worker.fingerprint}});const spawnedTask=m.tasks.find(t=>t.id===spawned.task_id);return{task_id:spawnedTask?.id??spawned.task_id,worker_id:spawned.id,session_id:spawned.session_id,model:spawned.model,skills:spawned.loaded_skills,selection_reason:[...routed.reason,...selected.reason,'deduped:existing-spawn',...selected.fallbackReasons.map(x=>`${x.model}:${x.reason}`),...skillPlan.reason],readiness:'READY',preconditions:preflight.items}}return{task_id:task.id,worker_id:spawned.id,session_id:spawned.session_id,model:spawned.model,skills:spawned.loaded_skills,selection_reason:[...routed.reason,...selected.reason,...selected.fallbackReasons.map(x=>`${x.model}:${x.reason}`),...skillPlan.reason],readiness:'READY',preconditions:preflight.items}
  }
  async reconcileUserConstraint(m:MissionState,text:string):Promise<number>{
    if(m.status!=='active'||m.user_interrupted)return 0
    let reconciled=0
    for(const worker of m.workers.filter(w=>['created','queued','starting','busy','ready'].includes(w.status))){
      const task=m.tasks.find(t=>t.id===worker.task_id);if(!task)continue
      task.constraints??=[];if(!task.constraints.includes(text))task.constraints.push(text);task.updated_at=Date.now()
      // Queued/not-running work will build its handoff lazily from task.constraints.
      if(!worker.session_id||['created','queued','ready'].includes(worker.status)){worker.generation_at_spawn=m.generation;worker.parent_mission_id=m.mission_id;appendLedger(m,'worker.constraint-updated',{task_id:task.id,worker_id:worker.id,payload:{mode:'deferred',generation:m.generation,constraint:text.slice(0,300)}});reconciled++;continue}
      const oldSession=worker.session_id,model=worker.model,variant=worker.model_variant
      // A restrictive user constraint invalidates the in-flight prompt. Abort and rebase the SAME
      // task/worker identity onto a fresh child session so late idle/result callbacks from the old
      // session cannot be mistaken for work performed under the new constraint.
      this.registry.delete(worker.id)
      try{await abortSession(this.client,oldSession)}catch{}
      try{
        const child=await createChildSession(this.client,m.session_id,`Hi · ${worker.role} · constraint update · ${task.objective.slice(0,45)}`,worker.role,model==='host-default'?undefined:model,variant)
        if(!child?.id)throw new Error('Constraint rebase child session id missing')
        worker.session_id=child.id;recordPreexistingUserBaseline(m,await this.captureNativeDiff(worker,'baseline'));worker.parent_mission_id=m.mission_id;worker.generation_at_spawn=m.generation;worker.status='busy';worker.started_at=Date.now();task.status='running';this.registry.set(worker)
        const handoff=clipText([ownershipContract('child',worker.loaded_skills),`Hi USER CONSTRAINT UPDATE for existing task ${task.id}.`,`OBJECTIVE: ${task.objective}`,`SCOPE: ${task.scope.join(', ')||'bounded by objective'}`,`CURRENT USER CONSTRAINTS: ${task.constraints.join(' | ')}`,`OBSERVED CHANGED FILES SO FAR: ${m.changed_files.slice(-30).join(', ')||'none'}`,'The previous child session was aborted because the user changed constraints. The latest constraint supersedes conflicting prior instructions. Do not write to prohibited surfaces. If prohibited files were already changed, report that explicitly; do not conceal or assume those edits are acceptable. Reconcile the existing task under the new constraint with the minimum safe change.','Return the normal structured WorkerResult.'].join('\n'),DEFAULT_CONTEXT_BUDGET.max_handoff_chars)
        await this.sendProviderPrompt(child.id,handoff,worker.role,model==='host-default'?undefined:model,variant,promptToolOverrides(task.execution_profile?.tools??[]))
        appendLedger(m,'worker.constraint-rebased',{task_id:task.id,worker_id:worker.id,payload:{from_session:oldSession,to_session:worker.session_id,generation:m.generation,constraint:text.slice(0,300)}});void this.events?.(runtimeSignal('worker.constraint-rebased',m.mission_id,{task_id:task.id,worker_id:worker.id,payload:{generation:m.generation}}));reconciled++
      }catch(error){this.scheduler.release(worker.id);worker.status='ready';task.status=task.result?'waiting':'blocked';this.registry.set(worker);appendLedger(m,'worker.constraint-rebase.failed',{task_id:task.id,worker_id:worker.id,payload:{from_session:oldSession,error:String(error),generation:m.generation}})}
    }
    syncMissionGates(m);return reconciled
  }
  async noteNativeWriteSet(m:MissionState,workerID:string,files:string[],source='session-diff',stateHash?:string):Promise<void>{
    const worker=m.workers.find(w=>w.id===workerID);if(!worker||!files.length)return
    worker.write_set=[...new Set([...(worker.write_set??[]),...files])].slice(0,300);if(stateHash)worker.native_state_hash=stateHash;markMutation(m,files,source);if(READ_ONLY.has(worker.role))return
    for(const other of m.workers){
      if(other.id===worker.id||READ_ONLY.has(other.role)||!(other.write_set??[]).length||!['starting','busy'].includes(other.status)||!['starting','busy'].includes(worker.status))continue
      const overlap=(worker.write_set??[]).filter(x=>(other.write_set??[]).includes(x));if(!overlap.length)continue
      // The worker whose overlapping write is observed second is quarantined. The already-running
      // writer is allowed to finish, then the quarantined task resumes in the SAME child session
      // after an explicit dependency gate. This prevents blind concurrent merging while preserving
      // task/worker identity and context.
      const winner=other,loser=worker,winnerTask=m.tasks.find(t=>t.id===winner.task_id),loserTask=m.tasks.find(t=>t.id===loser.task_id);if(!winnerTask||!loserTask)continue
      const pair=[winner.id,loser.id].sort().join(':');const marker=`parallel-write-conflict:${pair}:${overlap.slice(0,8).sort().join(',')}`
      if(!m.blockers.includes(marker))m.blockers.push(marker)
      if(!loserTask.dependencies.includes(winnerTask.id))loserTask.dependencies.push(winnerTask.id)
      loserTask.result={status:'FIX_REQUIRED',summary:`Runtime write conflict detected with ${winner.id}; serialized reconciliation required.`,changed_files:[...new Set(loser.write_set??[])],evidence:[],open_issues:[marker],needs_context:[]}
      loserTask.updated_at=Date.now();loser.status='queued';loser.completed_at=undefined
      this.scheduler.release(loser.id);this.registry.delete(loser.id);if(loser.session_id)try{await abortSession(this.client,loser.session_id)}catch{}
      const resume=async()=>{const model=loser.model,provider=providerOf(model),capacity=this.scheduler.canStart(loser.id,provider,model==='host-default'?undefined:model);if(!capacity.ok)throw new Error(`Conflict resume capacity unavailable: ${capacity.reason}`);this.scheduler.acquire(loser.id,provider,model==='host-default'?undefined:model);loser.status='busy';loser.started_at=Date.now();loser.generation_at_spawn=m.generation;loser.parent_mission_id=m.mission_id;loserTask.status='running';this.registry.set(loser);if(!loser.session_id)throw new Error('Conflict resume child session missing');await this.sendProviderPrompt(loser.session_id,clipText([`Hi runtime write-conflict reconciliation for existing task ${loserTask.id}.`,`Conflicting task ${winnerTask.id} has completed before this resume gate opened.`,`Conflicting files: ${overlap.join(', ')}`,`Current task objective: ${loserTask.objective}`,`Current user constraints: ${(loserTask.constraints??[]).join(' | ')||'none'}.`,'Inspect the current diff/state first. Preserve valid work from the completed task. Reconcile only this task sequentially; do not blindly overwrite or restart planning. Re-run the required scoped verification and return the structured WorkerResult.'].join('\n'),DEFAULT_CONTEXT_BUDGET.max_handoff_chars),loser.role,model==='host-default'?undefined:model,loser.model_variant,promptToolOverrides(loserTask.execution_profile?.tools??[]));appendLedger(m,'parallel.write-conflict.resumed',{task_id:loserTask.id,worker_id:loser.id,payload:{after_task:winnerTask.id,files:overlap.slice(0,30)}});return loser}
      this.queueTask(m,loser,resume)
      appendLedger(m,'parallel.write-conflict.quarantined',{task_id:loserTask.id,worker_id:loser.id,payload:{winner_worker_id:winner.id,winner_task_id:winnerTask.id,files:overlap.slice(0,30),policy:'abort-later-writer-then-serialize'}});void this.events?.(runtimeSignal('parallel.write-conflict',m.mission_id,{task_id:loserTask.id,worker_id:loser.id,payload:{other_worker_id:winner.id,files:overlap.slice(0,30),action:'quarantined'}}));break
    }
    syncMissionGates(m)
  }
  noteNativeStatus(m:MissionState,workerID:string,status:string):void{const worker=m.workers.find(w=>w.id===workerID);if(!worker)return;appendLedger(m,'worker.native-status',{worker_id:worker.id,payload:{status}})}
  applyResult(m:MissionState,workerID:string,result:WorkerResult):void{
    const worker=m.workers.find(w=>w.id===workerID);if(!worker)return
    if(worker.generation_at_spawn!==undefined&&worker.generation_at_spawn!==m.generation){appendLedger(m,'worker.result.stale-generation-ignored',{worker_id:worker.id,payload:{worker_generation:worker.generation_at_spawn,mission_generation:m.generation}});return}
    const task=m.tasks.find(t=>t.id===worker.task_id);if(!task)return
    const digest=resultDigest(result);if(worker.last_result_digest===digest){appendLedger(m,'worker.result.duplicate-ignored',{task_id:task.id,worker_id:worker.id,payload:{digest}});return}
    worker.last_result_digest=digest;worker.last_result_at=Date.now()
    const observedMutationDuringWorker=Boolean(worker.started_at&&m.evidence.last_mutation_at&&m.evidence.last_mutation_at>=worker.started_at)
    const previousIssues=task.result?.open_issues??[];if(previousIssues.length)m.blockers=m.blockers.filter(b=>!previousIssues.includes(b)||m.tasks.some(other=>other.id!==task.id&&(other.result?.open_issues??[]).includes(b)))

    const previousCollateral=[...(task.diff_cleanliness?.collateral??[])]
    const ownership=READ_ONLY.has(worker.role)&&result.changed_files.length
      ? {outside:[...result.changed_files],accepted:[],collateral:[...result.changed_files]}
      : assessDiffOwnership(task,result)
    if(ownership.accepted.length){
      task.diff_cleanliness={collateral:previousCollateral,accepted_expansions:[...new Set([...(task.diff_cleanliness?.accepted_expansions??[]),...ownership.accepted])]}
    }
    const cleanlinessMarker=ownership.collateral.length?`diff-cleanliness:${task.id}:${ownership.collateral.slice(0,12).sort().join(',')}`:undefined
    const claimedReverted=previousCollateral.filter(file=>!new Set(result.changed_files.map(normFile)).has(normFile(file)))
    const verifiedReverts=new Set((task.diff_cleanliness?.native_verified_reverts??[]).map(normFile))
    const unverifiedReverts=!cleanlinessMarker?claimedReverted.filter(file=>!verifiedReverts.has(normFile(file))):[]
    const cleanupMarker=unverifiedReverts.length?`cleanup-unverified:${task.id}:${unverifiedReverts.slice(0,12).sort().join(',')}`:undefined
    const effectiveResult:WorkerResult=cleanlinessMarker?{
      ...result,
      status:'FIX_REQUIRED',
      summary:`Diff cleanliness reconciliation required before completion. Unowned/collateral changed files: ${ownership.collateral.slice(0,12).join(', ')}.`,
      open_issues:[...new Set([...result.open_issues,cleanlinessMarker])],
      needs_context:[...new Set([...result.needs_context,'diff-cleanliness-reconcile: inspect current diff; revert collateral files or explicitly justify necessary bounded scope expansion'])]
    }:cleanupMarker?{
      ...result,
      status:'FIX_REQUIRED',
      summary:`Cleanup was reported but deterministic native diff evidence is still required for: ${unverifiedReverts.slice(0,12).join(', ')}.`,
      open_issues:[...new Set([...result.open_issues,cleanupMarker])],
      needs_context:[...new Set([...result.needs_context,'cleanup-verification: do not close collateral-diff blockers from WorkerResult claims alone; provide native/session diff confirmation'])]
    }:result
    if(cleanlinessMarker){
      task.diff_cleanliness={collateral:[...ownership.collateral],accepted_expansions:[...(task.diff_cleanliness?.accepted_expansions??[])]}
      appendLedger(m,'diff.cleanliness.blocked',{task_id:task.id,worker_id:worker.id,payload:{collateral:ownership.collateral.slice(0,40),outside:ownership.outside.slice(0,40),role:worker.role}})
      void this.events?.(runtimeSignal('diff.cleanliness.blocked',m.mission_id,{task_id:task.id,worker_id:worker.id,payload:{collateral:ownership.collateral.slice(0,40)}}))
    }else if(previousCollateral.length&&!cleanupMarker){
      const stillChanged=new Set(result.changed_files.map(normFile))
      const reverted=previousCollateral.filter(file=>!stillChanged.has(normFile(file))&&verifiedReverts.has(normFile(file)))
      if(reverted.length){
        m.changed_files=m.changed_files.filter(file=>!reverted.includes(file)||m.tasks.some(t=>t.id!==task.id&&(t.result?.changed_files??[]).includes(file)))
        appendLedger(m,'diff.cleanliness.resolved',{task_id:task.id,worker_id:worker.id,payload:{reverted:reverted.slice(0,40),source:'native-session-diff'}})
      }
      const unresolved=previousCollateral.filter(file=>!reverted.includes(file))
      task.diff_cleanliness={collateral:unresolved,accepted_expansions:[...(task.diff_cleanliness?.accepted_expansions??[])],native_verified_reverts:[]}
    }

    applyWorkerResult(m,task,worker,effectiveResult);this.scheduler.release(worker.id);this.registry.delete(worker.id)
    void this.events?.(runtimeSignal('worker.completed',m.mission_id,{task_id:task.id,worker_id:worker.id,payload:{status:effectiveResult.status}}))
    if(effectiveResult.open_issues.some(x=>String(x).toUpperCase().includes('USER_ACTION_REQUIRED'))){m.status='waiting-user';appendLedger(m,'user.action.required',{task_id:task.id,worker_id:worker.id,payload:{kind:'worker-gate',summary:effectiveResult.summary.slice(0,500)}})}
    const fallbackMutation=effectiveResult.changed_files.length>0&&!observedMutationDuringWorker;if(fallbackMutation)markMutation(m,effectiveResult.changed_files,'worker-result-fallback')
    const replan=replanVerificationForChangedSurface(m,task,effectiveResult.changed_files,collectRepoContext(this.projectRoot));if(replan.changed){appendLedger(m,'verification.replanned',{task_id:task.id,worker_id:worker.id,payload:{changed_files:effectiveResult.changed_files.slice(0,30),added_kinds:replan.addedKinds,scope_expanded:replan.scopeExpanded,risk_escalated:replan.riskEscalated,reason:replan.reason}});void this.events?.(runtimeSignal('verification.replanned',m.mission_id,{task_id:task.id,worker_id:worker.id,payload:{added_kinds:replan.addedKinds,scope_expanded:replan.scopeExpanded,risk_escalated:replan.riskEscalated}}))}
    if(ownership.accepted.length){task.scope=[...new Set([...task.scope,...ownership.accepted])];appendLedger(m,'task.scope-expanded',{task_id:task.id,worker_id:worker.id,payload:{files:ownership.accepted.slice(0,40),policy:'bounded-explicit-ownership'}})}
    const evidenceSource=READ_ONLY.has(worker.role)?`worker:${worker.id}:reviewer`:`worker:${worker.id}`
    for(const e of effectiveResult.evidence)addEvidence(m,{kind:e.kind,summary:e.summary,scope:e.scope??effectiveResult.changed_files,source:evidenceSource,source_session_id:worker.session_id,source_state_hash:worker.native_state_hash,task_id:task.id,obligation_ids:task.obligation_ids,pass:e.pass,outcome:e.outcome,reason:e.reason,invalidated_at:(cleanlinessMarker||fallbackMutation&&!READ_ONLY.has(worker.role))?(m.evidence.last_mutation_at??Date.now()):undefined})
    const ownsReviewObligation=task.obligation_ids.some(id=>m.obligations.some(o=>o.id===id&&o.kind==='review'))
    if(effectiveResult.status==='DONE'&&READ_ONLY.has(worker.role)&&ownsReviewObligation&&!effectiveResult.evidence.some(e=>e.kind==='review-evidence'))addEvidence(m,{kind:'review-evidence',summary:effectiveResult.summary||`Independent ${worker.role} completed owned review task`,scope:effectiveResult.changed_files,source:evidenceSource,source_session_id:worker.session_id,source_state_hash:worker.native_state_hash,task_id:task.id,obligation_ids:task.obligation_ids,pass:true,outcome:'passed'})
    if(effectiveResult.status==='DONE'){
      const now=Date.now();if(worker.role==='repository-explorer'&&m.intent.ambiguity!=='none'){m.intent.ambiguity='none';appendLedger(m,'intent.ambiguity.resolved',{task_id:task.id,worker_id:worker.id,payload:{source:'repository-explorer-result'}})}
      for(const id of task.obligation_ids){const owned=m.obligations.find(o=>o.id===id&&o.status==='open');if(!owned)continue;if(owned.kind==='verification'){if(verificationSatisfied(m,owned.id).ok){owned.status='closed';owned.closedAt=now}}else{owned.status='closed';owned.closedAt=now}if(owned.status==='closed')appendLedger(m,'obligation.closed',{task_id:task.id,worker_id:worker.id,payload:{obligation:owned.id,owner:'task'}})}
    }
    syncMissionGates(m);this.drainQueue()
  }
  async recoverStagnation(m:MissionState,level:number):Promise<boolean>{
    if(![1,2].includes(level)||m.status!=='active'||m.user_interrupted)return false
    const worker=[...m.workers].reverse().find(w=>Boolean(w.session_id)&&!['failed','cancelled','busy','starting','queued'].includes(w.status))
    if(!worker?.session_id)return false
    const task=m.tasks.find(t=>t.id===worker.task_id);if(!task)return false
    let model=worker.model,variant=worker.model_variant,action='same-worker-resume'
    if(level===2){
      const stronger:Record<Category,Category>={quick:'standard',standard:'deep',visual:'deep',deep:'critical',critical:'critical'}
      const target=stronger[worker.category]
      const selected=resolveModel(target,this.getModels(),this.getConfig(),undefined,worker.role,this.getHostConfig(),missionModelFeedback(m))
      const next=[selected.primary,...selected.fallbacks].find(x=>Boolean(x)&&x!==worker.model)
      if(!next)return false
      model=next;variant=next===selected.primary?selected.primaryVariant:selected.fallbackVariants[next];action='model-escalation'
    }
    const capacity=this.scheduler.canStart(worker.id,providerOf(model),model==='host-default'?undefined:model);if(!capacity.ok)return false
    try{
      this.scheduler.acquire(worker.id,providerOf(model),model==='host-default'?undefined:model)
      const previous=worker.model
      worker.model=model;worker.model_variant=variant;worker.generation_at_spawn=m.generation;worker.parent_mission_id=m.mission_id;worker.status='busy';task.status='running';this.registry.set(worker)
      const instruction=level===1
        ?'Hi stagnation recovery: continue the SAME task/session with one narrowly scoped corrective attempt. Preserve completed work and evidence. Do not restart planning.'
        :`Hi stagnation recovery: continue the SAME task/session with policy escalation from ${previous??'default'} to ${model??'default'}. Preserve completed work and evidence. Do not restart planning.`
      await this.sendProviderPrompt(worker.session_id,clipText(`${instruction}\nReturn the normal structured WorkerResult.`,DEFAULT_CONTEXT_BUDGET.max_handoff_chars),worker.role,model==='host-default'?undefined:model,variant,promptToolOverrides(task.execution_profile?.tools??[]))
      appendLedger(m,'worker.stagnation-recovery',{task_id:task.id,worker_id:worker.id,payload:{level,action,from:previous,to:model,variant,generation:m.generation}})
      void this.events?.(runtimeSignal('worker.recovered',m.mission_id,{task_id:task.id,worker_id:worker.id,payload:{level,action,from:previous,to:model,variant}}))
      return true
    }catch(error){
      this.scheduler.release(worker.id);worker.status='ready';task.status=task.result?.status==='DONE'?'completed':task.result?'waiting':'blocked';this.registry.set(worker)
      appendLedger(m,'worker.stagnation-recovery.failed',{task_id:task.id,worker_id:worker.id,payload:{level,action,error:String(error)}})
      return false
    }
  }
  async recoverRuntimeFailure(m:MissionState,workerID:string,error:string):Promise<boolean>{const worker=m.workers.find(w=>w.id===workerID);if(!worker)return false;const task=m.tasks.find(t=>t.id===worker.task_id),failure=classifyWorkerFailure(error);worker.last_runtime_failure_kind=failure.kind;worker.runtime_fallback_exhausted=false;appendLedger(m,'worker.failure.classified',{task_id:task?.id,worker_id:worker.id,payload:{kind:failure.kind,stagnation:failure.stagnation,retryable:failure.retryable,reason:failure.reason}});if(!failure.retryable||!['provider-transport','tool-incompatibility','context-overflow'].includes(failure.kind)||!worker.session_id)return false;this.scheduler.release(worker.id);const candidates=worker.fallbacks.filter(x=>x&&x!==worker.model);for(const model of candidates){const runtimeCandidate=runtimeModelCandidateStatus(model,this.getModels(),this.getConfig(),this.getHostConfig());if(!runtimeCandidate.ok){appendLedger(m,'worker.runtime-fallback.skipped',{task_id:task?.id,worker_id:worker.id,payload:{model,reason:runtimeCandidate.reason,failure_class:failure.kind,phase:'runtime-policy-revalidation'}});continue}const provider=providerOf(model),capacity=this.scheduler.canStart(worker.id,provider,model==='host-default'?undefined:model);if(!capacity.ok){appendLedger(m,'worker.runtime-fallback.skipped',{task_id:task?.id,worker_id:worker.id,payload:{model,reason:capacity.reason,failure_class:failure.kind}});continue}try{this.scheduler.acquire(worker.id,provider,model==='host-default'?undefined:model);const variant=task?.execution_profile?.fallback_variants?.[model];const previous=worker.model,fallbackReason=task?.execution_profile?.fallback_reasons?.find(x=>x.model===model)?.reason??`runtime fallback after ${failure.kind}`;worker.model=model;worker.model_variant=variant;worker.fallback_history=[...(worker.fallback_history??[]),{from:previous,to:model,variant,reason:`${fallbackReason}; failure=${failure.kind}`,phase:'runtime',at:Date.now()}];worker.status='busy';worker.runtime_recovery_pending=true;worker.runtime_recovery_attempt=(worker.runtime_recovery_attempt??0)+1;if(task)task.status='running';await this.sendProviderPrompt(worker.session_id,clipText([`Hi native runtime recovery for task ${task?.id??worker.task_id}.`,`Failure class: ${failure.kind}.`,`Previous model/provider failed: ${previous??'unknown'}.`,`Continue the SAME task/session with fallback model. Preserve completed work. Do not restart planning. Return the normal structured WorkerResult.`].join('\n'),DEFAULT_CONTEXT_BUDGET.max_handoff_chars),worker.role,model==='host-default'?undefined:model,variant,promptToolOverrides(task?.execution_profile?.tools??[]));appendLedger(m,'worker.runtime-fallback',{task_id:task?.id,worker_id:worker.id,payload:{from:previous,to:model,variant,reason:fallbackReason,failure_class:failure.kind,attempt:worker.runtime_recovery_attempt}});return true}catch(nextError){worker.runtime_recovery_pending=false;this.scheduler.release(worker.id);appendLedger(m,'worker.runtime-fallback.failed',{task_id:task?.id,worker_id:worker.id,payload:{model,error:String(nextError),failure_class:failure.kind}})}}worker.runtime_fallback_exhausted=true;m.stagnation_count=0;const blocker=`provider-failure:${failure.kind}:${worker.model??'unknown'}`;m.blockers=[...new Set([...m.blockers,blocker])];if(task){task.status='blocked';task.updated_at=Date.now();task.result={status:'BLOCKED',summary:'Runtime provider/model fallback chain exhausted.',changed_files:[],evidence:[],open_issues:[blocker],needs_context:['provider/model availability or alternate execution path']}}appendLedger(m,'worker.runtime-fallback.exhausted',{task_id:task?.id,worker_id:worker.id,payload:{failure_class:failure.kind,attempted:[worker.model,...candidates].filter(Boolean)}});return false}
  fail(m:MissionState,workerID:string,error:string):void{const worker=m.workers.find(w=>w.id===workerID);if(!worker)return;if(worker.generation_at_spawn!==undefined&&worker.generation_at_spawn!==m.generation){appendLedger(m,'worker.failure.stale-generation-ignored',{worker_id:worker.id});return}const task=m.tasks.find(t=>t.id===worker.task_id),permissionFailure=worker.last_runtime_failure_kind==='permission',marker=permissionFailure?`permission-failure:${worker.id}`:error;worker.status='failed';worker.completed_at=Date.now();this.scheduler.release(worker.id);this.registry.delete(worker.id);if(permissionFailure)m.stagnation_count=0;if(task){task.status='failed';task.updated_at=Date.now();task.result={status:'FAILED',summary:error,changed_files:[],evidence:[],open_issues:[marker],needs_context:permissionFailure?['resolve OpenCode permission/authority and explicitly resume the mission']:[]}}m.blockers=[...new Set([...m.blockers,marker])];appendLedger(m,'worker.failed',{task_id:task?.id,worker_id:worker.id,payload:{error,failure_class:worker.last_runtime_failure_kind??'unknown',blocker:marker}});void this.events?.(runtimeSignal('worker.failed',m.mission_id,{task_id:task?.id,worker_id:worker.id,payload:{error,failure_class:worker.last_runtime_failure_kind??'unknown'}}));syncMissionGates(m);this.drainQueue()}
  peek(m:MissionState,id:string):any{const task=m.tasks.find(t=>t.id===id),worker=m.workers.find(w=>w.id===id||w.id===task?.worker_id);return{task,worker}}
  list(m:MissionState):any[]{return m.tasks.map(t=>({task:t,worker:m.workers.find(w=>w.id===t.worker_id)}))}
  async cancelAll(m:MissionState):Promise<number>{let n=0;for(const w of [...m.workers])if(['created','queued','starting','ready','busy'].includes(w.status))if(await this.cancel(m,w.id))n++;return n}
  async cancel(m:MissionState,id:string):Promise<boolean>{const task=m.tasks.find(t=>t.id===id),worker=m.workers.find(w=>w.id===id||w.id===task?.worker_id);if(!worker)return false;if(worker.session_id)try{await abortSession(this.client,worker.session_id)}catch{}worker.status='cancelled';this.scheduler.release(worker.id);const t=m.tasks.find(x=>x.id===worker.task_id);if(t)t.status='cancelled';this.registry.delete(worker.id);this.#queue=this.#queue.filter(q=>q.worker.id!==worker.id);appendLedger(m,'worker.cancelled',{task_id:t?.id,worker_id:worker.id});syncMissionGates(m);this.drainQueue();return true}
}
