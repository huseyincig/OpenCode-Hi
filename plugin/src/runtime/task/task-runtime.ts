import type { HiConfig } from '../../config/schema.js'
import type { Category,ExecutionProfile,MissionState,WorkerResult,WorkerState } from '../mission/types.js'
import { resolveCategory } from '../routing/category.js'
import { resolveModel,runtimeModelCandidateStatus,type AvailableModel } from '../routing/model-resolver.js'
import { deriveMissionModelFeedback } from '../routing/model-feedback.js'
import { methodologySkillCandidates,resolveSkillPlan } from '../skills/registry.js'
import { resolveSkillPermissionMap,resolveSkillToolEnabled } from '../skills/permissions.js'
import { createTask,createWorker,beginWorkerAttempt,workerFingerprint } from '../worker/worker-runtime.js'
import { BackgroundRegistry } from '../background/registry.js'
import { workerHandoffText } from './contracts.js'
import { appendLedger } from '../ledger/ledger.js'
import { ConcurrencyScheduler } from '../scheduler/concurrency.js'
import { routeCapabilities } from '../routing/capability-router.js'
import { verificationEconomyInstruction } from '../verification/policy.js'
import { targetedVerificationHint } from '../verification/discovery.js'
import { bindMethodologyNeeds,methodologyNames } from '../methodology/activation.js'
import { methodologyCatalog } from '../methodology/catalog.js'
import { methodologyProvenance,ownershipContract } from '../skills/methodology.js'
import { DEFAULT_CONTEXT_BUDGET,clipList,clipText } from '../context/budget.js'
import type { RuntimeSignalSink } from '../events/event-sink.js'
import { runtimeSignal } from '../events/event-sink.js'
import { syncMissionGates } from '../gates/gates.js'
import { recordPreexistingUserBaseline } from '../safety/staging-safety.js'
import { evaluateTaskPreconditions,TaskPreconditionError } from '../readiness/preconditions.js'
import { effectiveExecutionSurface,promptToolOverrides } from '../routing/execution-profile.js'
import { HI_BROWSER_EXECUTION_TOOL_IDS } from '../browser/executor.js'
import { ProjectMethodologyLearningStore } from '../project-intelligence/methodology-learning.js'
import { executionProfileFor } from '../../config/execution-policy.js'
import { applyAdmittedProjectMethodologyPermissions } from '../methodology/host-permissions.js'
import { isHiChildRole,isHiReadOnlyChildRole,isHiReviewerRole,roleCanOwnObligation } from '../roles/catalog.js'
import { renderSemanticContext,semanticContextsForTargets } from '../semantic/typescript-context.js'
import { createRuntimeScopedStores,type RuntimeScopedStores } from '../application/runtime-scoped-stores.js'
import { ChildExecutionCoordinator } from './child-execution-coordinator.js'
import { TaskResultReconciler } from './task-result-reconciler.js'
import { TaskRecoveryCoordinator } from './task-recovery-coordinator.js'
import type { WorkspaceRuntime } from '../workspace/runtime.js'
import type { ChildWorkspaceBinding } from './child-execution-coordinator.js'
import type { ChildSessionPort } from '../host/port.js'
import type { HostCapabilityContract } from '../../contracts/host-capability.js'
import { taskRuntimeAdmittedModel,taskRuntimeUnitDecision,reserveTaskRuntimeDispatch,bindTaskRuntimeHost,releaseTaskRuntimeReservation,reconcileStoppedTaskRuntimeRestart } from '../scheduler/task-runtime-adapter.js'
import { bindWorkerUsageObservation } from '../economics/usage-runtime.js'
import type { HostUsageObservation } from '../../contracts/execution-usage.js'

export interface StartTaskInput{objective?:string;role?:string;category?:Category;scope?:string[];dependencies?:string[];requiredEvidence?:string[];obligationIds?:string[];model?:string;modelVariant?:string;relevantContext?:string[];contextArtifactIds?:string[];constraints?:string[];forkFromSession?:string;isolationRequired?:boolean;isolationReason?:string;resumeTaskId?:string}
const CATEGORIES=new Set(['quick','standard','deep','visual','critical'])
const MAX_QUEUE=32
function inferObligationIds(m:MissionState,role:string,requiredEvidence:string[],explicit:string[]=[]):string[]{
  const requested=[...new Set(explicit)].map(id=>m.execution.obligations.find(o=>o.id===id&&o.status==='open')).filter(Boolean) as MissionState['execution']['obligations']
  const disallowed=requested.filter(o=>!roleCanOwnObligation(role,o.kind));if(disallowed.length)throw new Error(`Role ${role} cannot own obligation(s): ${disallowed.map(o=>`${o.id}:${o.kind}`).join(', ')}`)
  if(requested.length)return requested.map(o=>o.id)
  const kinds:string[]=[]
  if(role==='coder')kinds.push('implementation')
  if(['repository-explorer','architect'].includes(role)||role==='coder'&&['bug-fix','performance'].includes(m.identity.intent.taskKind))kinds.push('analysis')
  if(isHiReviewerRole(role))kinds.push('review')
  if(requiredEvidence.length)kinds.push('verification')
  const out:string[]=[]
  for(const kind of [...new Set(kinds)].filter(k=>roleCanOwnObligation(role,k))){const candidates=m.execution.obligations.filter(o=>o.kind===kind&&o.status==='open');if(candidates.length===1)out.push(candidates[0].id)}
  return [...new Set(out)]
}
function providerOf(model:string|undefined):string|undefined{return model&&model!=='host-default'&&model.includes('/')?model.slice(0,model.indexOf('/')):undefined}
interface QueueEntry{mission:MissionState;worker:WorkerState;run:()=>Promise<WorkerState>;created:number}
export class TaskRuntime{
  #queue:QueueEntry[]=[];#draining=false;readonly #methodologyLearning:ProjectMethodologyLearningStore;readonly #child:ChildExecutionCoordinator;readonly #results:TaskResultReconciler;readonly #recovery:TaskRecoveryCoordinator
  readonly #scopedStores:RuntimeScopedStores
  constructor(private childHost:ChildSessionPort,private registry:BackgroundRegistry,private scheduler:ConcurrencyScheduler,private projectRoot:string,private readonly hiRoot:string,private getConfig:()=>HiConfig,private getModels:()=>AvailableModel[],private getHostConfig:()=>Record<string,unknown>,private events?:RuntimeSignalSink,private readonly hostCapabilitySource:(()=>readonly HostCapabilityContract[])|readonly HostCapabilityContract[]=[],scopedStores?:RuntimeScopedStores,private readonly workspaceRuntime?:WorkspaceRuntime,private readonly extraHostResources:()=>ReadonlySet<string>=()=>new Set()){ this.#scopedStores=scopedStores??createRuntimeScopedStores(projectRoot,hiRoot);this.#methodologyLearning=new ProjectMethodologyLearningStore(projectRoot);this.#child=new ChildExecutionCoordinator(childHost,registry);this.#results=new TaskResultReconciler(scheduler,registry,projectRoot,events,this.#methodologyLearning,this.#child,(m,w,run)=>this.queueTask(m,w,run),()=>this.drainQueue(),this.#scopedStores);this.#recovery=new TaskRecoveryCoordinator(scheduler,registry,projectRoot,getConfig,getModels,getHostConfig,events,this.#child,()=>this.drainQueue(),(m,taskID)=>this.workspaceBinding(m,taskID))}
  private async sendProviderPrompt(sessionID:string,text:string,role?:string,model?:string,variant?:string,tools?:Record<string,boolean>):Promise<unknown>{return this.#child.sendProviderPrompt(sessionID,text,role,model,variant,tools)}
  private recordModelProjection(worker:WorkerState,model?:string,variant?:string):void{this.#child.recordModelProjection(worker,model,variant)}
  private async abortNativeSession(m:MissionState,sessionID:string,reason:string,workerID?:string,taskID?:string):Promise<boolean>{return this.#child.abortNativeSession(m,sessionID,reason,workerID,taskID)}
  private async captureNativeDiff(worker:WorkerState,phase:'baseline'|'final'):Promise<Record<string,string>|undefined>{return this.#child.captureNativeDiff(worker,phase)}
  async reconcileNativeResult(m:MissionState,workerID:string,result:WorkerResult):Promise<WorkerResult>{return this.#results.reconcileNativeResult(m,workerID,result)}
  noteUsage(m:MissionState,workerID:string,usage:HostUsageObservation):void{const worker=m.execution.workers.find(w=>w.id===workerID);if(worker)bindWorkerUsageObservation(m,worker,usage)}
  noteEffectiveModel(m:MissionState,workerID:string,observed?:{model?:string;variant?:string;source?:string}):{ok:boolean;expected?:string;observed?:string;reason:string}{return this.#child.noteEffectiveModel(m,workerID,observed)}
  resolveChildCallback(sessionID:string):WorkerState|undefined{return this.#child.resolveCallbackWorker(sessionID)}
  childCallbackDisposition(m:MissionState,worker:WorkerState){return this.#recovery.callbackDisposition(m,worker)}
  queueDepth():number{return this.#queue.length}
  private workspaceBinding(m:MissionState,taskID:string):ChildWorkspaceBinding|undefined{const required=m.execution.isolation_decisions.some(d=>d.required&&d.requested_by===`task:${taskID}`),lease=this.workspaceRuntime?.forTask(m,taskID);if(required&&(!lease||lease.status!=='ACTIVE'||lease.cleanup_state!=='ACTIVE'||!lease.host_workspace_id))throw new Error(`Required workspace lease is not active for task ${taskID}`);return lease?.host_workspace_id&&lease.status==='ACTIVE'&&lease.cleanup_state==='ACTIVE'?{workspaceID:lease.host_workspace_id,directory:lease.workspace_path}:undefined}
  async cleanupWorkspaceForTask(m:MissionState,taskID:string):Promise<boolean>{return this.workspaceRuntime?this.workspaceRuntime.cleanupTask(m,taskID):true}
  private failedDeps(m:MissionState,deps:string[]):string[]{return deps.filter(id=>{const status=m.execution.tasks.find(t=>t.id===id)?.status;return status==='failed'||status==='cancelled'})}
  private admittedModel(m:MissionState,worker:WorkerState,chain:string[]):string|undefined{return taskRuntimeAdmittedModel(m,worker,chain,this.scheduler)}
  private reserveExistingSessionAttempt(m:MissionState,worker:WorkerState,model:string|undefined):{ok:boolean;reason:string}{
    if(!model||!worker.session_id)return{ok:false,reason:'model-or-session-missing'}
    if(this.admittedModel(m,worker,[model])!==model)return{ok:false,reason:'scheduler-not-admitted'}
    const reservation=reserveTaskRuntimeDispatch(m,worker,model,this.scheduler);if(!reservation.accepted)return{ok:false,reason:reservation.reason}
    const provider=providerOf(model);if(!this.scheduler.acquire(worker.id,provider,model==='host-default'?undefined:model)){releaseTaskRuntimeReservation(m,worker.id);return{ok:false,reason:'legacy-resource-backstop'}}
    const bound=bindTaskRuntimeHost(m,worker.id,worker.session_id);if(!bound.accepted){this.scheduler.release(worker.id);releaseTaskRuntimeReservation(m,worker.id);return{ok:false,reason:bound.reason}}
    return{ok:true,reason:'reserved-and-bound'}
  }
  private queueTask(m:MissionState,worker:WorkerState,run:()=>Promise<WorkerState>):void{if(this.#queue.length>=MAX_QUEUE)throw new Error('Hi bounded dispatch queue is full');const t=m.execution.tasks.find(x=>x.id===worker.task_id);worker.status='queued';if(t)t.status='queued';if(!this.#queue.some(x=>x.worker.id===worker.id))this.#queue.push({mission:m,worker,run,created:Date.now()});this.registry.set(worker);appendLedger(m,'worker.queued',{task_id:t?.id,worker_id:worker.id,payload:{queue_depth:this.#queue.length}});void this.events?.(runtimeSignal('worker.queued',m.identity.mission_id,{task_id:t?.id,worker_id:worker.id,payload:{queue_depth:this.#queue.length}}));syncMissionGates(m)}
  private drainQueue():void{if(this.#draining)return;this.#draining=true;queueMicrotask(async()=>{try{let progress=true;while(progress){progress=false;for(let i=0;i<this.#queue.length;i++){const e=this.#queue[i],t=e.mission.execution.tasks.find(x=>x.id===e.worker.task_id),chain=[e.worker.model,...e.worker.fallbacks].filter((x):x is string=>Boolean(x));if(e.mission.identity.status!=='active'||e.mission.continuation.user_interrupted||e.worker.status==='cancelled'){this.#queue.splice(i--,1);continue}if(!t){this.#queue.splice(i--,1);continue}const decision=taskRuntimeUnitDecision(e.mission,e.worker,chain[0],this.scheduler),failed=decision?.disposition==='BLOCKED_DEPENDENCY'?decision.blockingDependencyIds.filter(id=>{const status=e.mission.execution.tasks.find(task=>task.id===id)?.status;return status==='failed'||status==='cancelled'}):[];if(failed.length){this.#queue.splice(i--,1);e.worker.status='failed';t.status='blocked';t.updated_at=Date.now();const reason=`dependency-unavailable:${failed.join(',')}`;t.result={status:'BLOCKED',summary:'Required dependency did not complete successfully.',changed_files:[],evidence:[],open_issues:[reason],needs_context:[]};e.mission.execution.blockers=[...new Set([...e.mission.execution.blockers,reason])];this.registry.delete(e.worker.id);await this.cleanupWorkspaceForTask(e.mission,t.id);appendLedger(e.mission,'worker.dependency-blocked',{task_id:t.id,worker_id:e.worker.id,payload:{dependencies:failed,source:'scheduler'}});void this.events?.(runtimeSignal('worker.dependency-blocked',e.mission.identity.mission_id,{task_id:t.id,worker_id:e.worker.id,payload:{dependencies:failed}}));syncMissionGates(e.mission);progress=true;continue}if(!this.admittedModel(e.mission,e.worker,chain))continue;this.#queue.splice(i--,1);progress=true;try{await e.run()}catch{}}}}finally{this.#draining=false}})}

  async start(m:MissionState,input:StartTaskInput={}):Promise<{task_id:string;worker_id:string;session_id?:string;model?:string;methodologies:string[];selection_reason:string[];readiness:'READY'|'WAIT';preconditions:Array<{id:string;decision:string;reason:string}>}>{
    if(m.identity.status!=='active'||m.continuation.user_interrupted)throw new Error('Mission is not active')
    if(m.identity.semantic_assessment.status!=='assessed')throw new Error('Hi semantic assessment is pending; assess mission intent before starting a worker')
    const objective=input.objective?.trim()||m.identity.objective
    const taskIntent=m.identity.intent
    const cfg=this.getConfig(),routingProfile=cfg.profile[executionProfileFor(cfg.executionPolicy,taskIntent)],routed=routeCapabilities(taskIntent,{specialistThreshold:routingProfile.specialistThreshold,reviewThreshold:routingProfile.reviewThreshold}),defaultCategory=resolveCategory(taskIntent),category=(CATEGORIES.has(String(input.category))?input.category:(routed.category??defaultCategory)) as Category,defaultRole=isHiChildRole(routed.role)?routed.role:'coder',role=isHiChildRole(String(input.role))?String(input.role):defaultRole
    const hostConfig=this.getHostConfig();applyAdmittedProjectMethodologyPermissions(hostConfig,this.projectRoot);const feedback=deriveMissionModelFeedback(m,role,category),selected=resolveModel(category,this.getModels(),this.getConfig(),input.model,role,hostConfig,feedback);if(selected.rejected.length)appendLedger(m,'model.policy.rejected',{payload:{items:selected.rejected.slice(0,20)}});if(selected.scores?.length)appendLedger(m,'model.scored',{payload:{role,category,top:selected.scores.slice(0,6),feedback}})
    const taskMethodologyNeeds=m.methodology.methodology_needs.filter(need=>(!need.task_id&&!need.obligation_id)||(need.obligation_id&&input.obligationIds?.includes(need.obligation_id))),catalog=methodologyCatalog(this.projectRoot),requestedMethodologyNames=methodologyNames(taskMethodologyNeeds),candidates=methodologySkillCandidates(requestedMethodologyNames,this.projectRoot,this.hiRoot,hostConfig,catalog),permissionMap=resolveSkillPermissionMap(hostConfig,role),skillToolEnabled=resolveSkillToolEnabled(hostConfig,role),surface=effectiveExecutionSurface(hostConfig,role,skillToolEnabled),hostCapabilities=typeof this.hostCapabilitySource==='function'?this.hostCapabilitySource():Array.isArray(this.hostCapabilitySource)?this.hostCapabilitySource:[],availableResources=new Set([...hostCapabilities.filter(item=>item.status==='SUPPORTED'&&item.runtime_health_required!==true).map(item=>`host-capability:${item.id}`),...this.extraHostResources()]),skillPlan=resolveSkillPlan(requestedMethodologyNames,candidates,permissionMap,skillToolEnabled,role,catalog,availableResources),methodologies=skillPlan.selected.map(s=>s.name),methodologyResourceFailures=skillPlan.outcomes.filter(item=>item.outcome==='resource-unavailable').map(item=>item.name)
    appendLedger(m,'skill.resolved',{payload:{role,requested:skillPlan.requested,outcomes:skillPlan.outcomes}});void this.events?.(runtimeSignal('skill.resolved',m.identity.mission_id,{payload:{role,requested:skillPlan.requested,outcomes:skillPlan.outcomes}}));if(skillPlan.missing.length)appendLedger(m,'skill.fallback',{payload:{missing:skillPlan.missing,requested:skillPlan.requested,skillToolEnabled}})
    const scope=input.scope??(isHiReadOnlyChildRole(role)&&m.vcs.changed_files.length?m.vcs.changed_files:taskIntent.likelyTargets??[]),dependencies=[...new Set(input.dependencies??[])];const unknownDependencies=dependencies.filter(id=>!m.execution.tasks.some(t=>t.id===id)),unavailableDependencies=this.failedDeps(m,dependencies),incompleteDependencies=dependencies.filter(id=>{const t=m.execution.tasks.find(x=>x.id===id);return Boolean(t)&&t!.status!=='completed'&&!unavailableDependencies.includes(id)})
    const requiredEvidence=input.requiredEvidence??m.execution.verification_policy.requiredKinds,obligationIds=inferObligationIds(m,role,requiredEvidence,input.obligationIds),isolationRequired=input.isolationRequired===true,isolationReason=String(input.isolationReason??'').trim();if(isolationRequired&&!isolationReason)throw new Error('Hi isolated task requires a bounded isolation reason');const constraints=[...new Set([...(m.execution.constraints??[]),...(input.constraints??[]),...(isolationRequired?['hi-isolation:git-worktree']:[])])],desiredFingerprint=workerFingerprint(role,category,selected.primary,taskIntent.taskKind,objective,{scope,constraints,dependencies,requiredEvidence,obligationIds}),existing=input.resumeTaskId?m.execution.workers.find(w=>w.task_id===input.resumeTaskId&&!['completed','failed','cancelled'].includes(w.status)):m.execution.workers.find(w=>w.fingerprint===desiredFingerprint&&!['completed','failed','cancelled'].includes(w.status));if(input.resumeTaskId&&!existing)throw new Error(`Hi task ${input.resumeTaskId} has no resumable worker`)
    const resumeCapable=Boolean(existing?.session_id),preflight=evaluateTaskPreconditions({role,implementation:role==='coder',dependencies:{unknown:unknownDependencies,failed:unavailableDependencies,incomplete:incompleteDependencies},modelAvailable:Boolean(selected.primary),native:{childSession:resumeCapable||this.childHost.capabilities.create,prompt:this.childHost.capabilities.prompt},hostConfig,methodologyResourceFailures,contractCriticalAmbiguity:m.identity.intent.ambiguity==='contract-critical',authorityRequired:false});appendLedger(m,'task.preflight',{payload:{role,decision:preflight.decision,resume_capable:resumeCapable,items:preflight.items.slice(0,12)}});void this.events?.(runtimeSignal('task.preflight',m.identity.mission_id,{payload:{role,decision:preflight.decision,resume_capable:resumeCapable,items:preflight.items.slice(0,12)}}));if(preflight.decision==='RESOLVE'||preflight.decision==='USER_ACTION_REQUIRED')throw new TaskPreconditionError(preflight)
    if(existing){this.workspaceBinding(m,existing.task_id);const oldTask=m.execution.tasks.find(t=>t.id===existing.task_id);if(existing.status==='ready'&&existing.session_id&&oldTask?.result&&['FIX_REQUIRED','NEEDS_CONTEXT','BLOCKED'].includes(oldTask.result.status)){const chain=[selected.primary,...selected.fallbacks].filter((x):x is string=>Boolean(x));if(existing.restart_reconcile_pending){const stopped=await this.abortNativeSession(m,existing.session_id,'restart-reconcile-quiesce',existing.id,oldTask?.id);if(!stopped){appendLedger(m,'scheduler.restart-reconcile-blocked',{task_id:oldTask?.id,worker_id:existing.id,payload:{reason:'abort-unavailable',session_id:existing.session_id}});throw new Error('Worker restart reconciliation requires a verified host abort before resume')}const reconciled=reconcileStoppedTaskRuntimeRestart(m,existing);if(!reconciled.accepted){appendLedger(m,'scheduler.restart-reconcile-blocked',{task_id:oldTask?.id,worker_id:existing.id,payload:{reason:reconciled.reason,session_id:existing.session_id}});throw new Error(`Worker restart scheduler reconciliation failed: ${reconciled.reason}`)}this.scheduler.release(existing.id);appendLedger(m,'scheduler.restart-reconciled',{task_id:oldTask?.id,worker_id:existing.id,payload:{session_id:existing.session_id,outcome:'terminal-aborted-before-resume'}})}const nextModel=this.admittedModel(m,existing,chain)??existing.model;if(!nextModel)throw new Error('Worker resume scheduler admission unavailable');const resumeAdmission=this.reserveExistingSessionAttempt(m,existing,nextModel);if(!resumeAdmission.ok)throw new Error(`Worker resume scheduler admission unavailable: ${resumeAdmission.reason}`);const previousModel=existing.model,attemptBaseline=await this.captureNativeDiff(existing,'baseline');if(!attemptBaseline)existing.native_diff_baseline=undefined;existing.native_diff_final=undefined;existing.model=nextModel;existing.generation_at_spawn=m.continuation.generation;existing.status='busy';existing.started_at=Date.now();oldTask.status='running';this.registry.set(existing);const issues=oldTask.result.open_issues.join(' | '),missing=oldTask.result.needs_context.join(' | '),freshEvidence=m.execution.evidence.items.filter(e=>!e.invalidated_at&&((e.outcome==='passed')||e.pass===true)&&(e.task_id===oldTask.id||e.obligation_ids?.some(id=>oldTask.obligation_ids.includes(id))||(e.scope??[]).some(file=>oldTask.scope.includes(file)))).slice(-8).map(e=>`${e.kind}: ${e.summary}`).join(' | '),reviewScope=isHiReadOnlyChildRole(existing.role)?`Scoped rereview only: previous findings=${issues||'none'}; changed scope=${m.vcs.changed_files.slice(-20).join(',')||'none'}; affected evidence=${freshEvidence||'none'}.`:'',resumeExitRequirements=existing.selected_methodologies.flatMap(name=>{const item=catalog.find(x=>x.name===name);return item?[`${name}: ${item.exitRequirements.join(', ')}`]:[]});const resumeVariant=nextModel===selected.primary?selected.primaryVariant:selected.fallbackVariants[nextModel];const protectedBaseline=Object.keys(m.vcs.preexisting_user_changes??{}).slice(0,60);beginWorkerAttempt(oldTask,existing);this.recordModelProjection(existing,nextModel,resumeVariant);await this.sendProviderPrompt(existing.session_id,clipText([`Hi corrective resume for existing task ${oldTask.id}.`,`Previous status: ${oldTask.result.status}.`,`Missing context: ${missing||'none'}.`,`Open issues: ${issues||'none'}.`,`Current user constraints: ${(oldTask.constraints??[]).join(' | ')||'none'}.`,`CURRENT FRESH EVIDENCE: ${freshEvidence||'none'}.`,`METHODOLOGY EXIT REQUIREMENTS: ${resumeExitRequirements.join(' | ')||'none'}.`,protectedBaseline.length?`PRE-EXISTING USER DIRTY BASELINE: ${protectedBaseline.join(', ')}. Cleanup means restore these paths to their exact worker-start baseline, NOT to HEAD. Never discard user-owned edits with git checkout/reset/restore.`:'Pre-existing user dirty baseline: none observed.',reviewScope,'Resume from current session context. Apply the smallest correction. Do not restart planning or create sub-orchestrators. Return the structured WorkerResult again.'].filter(Boolean).join('\n'),DEFAULT_CONTEXT_BUDGET.max_handoff_chars),existing.role,nextModel==='host-default'?undefined:nextModel,resumeVariant,promptToolOverrides(oldTask.execution_profile?.tools??[]));existing.model_variant=resumeVariant;existing.restart_reconcile_pending=false;appendLedger(m,nextModel!==previousModel?'worker.model-escalated':'worker.resumed',{task_id:oldTask.id,worker_id:existing.id,payload:{status:oldTask.result.status,model:nextModel}})}return{task_id:oldTask?.id??existing.task_id,worker_id:existing.id,session_id:existing.session_id,model:existing.model,methodologies:existing.selected_methodologies,selection_reason:['same-session worker reuse'],readiness:'READY',preconditions:preflight.items}}
    const requestedArtifactIds=[...new Set(input.contextArtifactIds??[])].slice(0,DEFAULT_CONTEXT_BUDGET.max_artifacts),unknownArtifactIds=requestedArtifactIds.filter(id=>!m.context.context_artifacts.some(a=>a.id===id));if(unknownArtifactIds.length)throw new Error(`Unknown context artifact id(s): ${unknownArtifactIds.join(', ')}`);const contextArtifactStore=this.#scopedStores.contextArtifacts,selectedContextHandles=requestedArtifactIds.map(id=>m.context.context_artifacts.find(a=>a.id===id)!).filter(Boolean),selectedContextReferences=selectedContextHandles.map(a=>{const durableId=a.uri?.startsWith('hi-artifact:')?a.uri.slice('hi-artifact:'.length):undefined,stored=durableId?contextArtifactStore.get(durableId):undefined;return{source_ref:a.uri??`mission-context:${a.id}`,reason:'explicit-task-selection',priority:'normal' as const,protection:'COMPRESSIBLE' as const,budget_cost:stored?Math.min(stored.content.length,3000):Math.min((a.summary??a.title??a.kind).length,3000),freshness:stored?.freshness??'UNKNOWN' as const,retention:'task' as const,privacy_class:stored?.privacy_class??'project-private' as const,kind:a.kind,title:a.title,summary:a.summary,content_hash:stored?.content_hash??a.sha256,source_handle_id:a.id}});const approvalGated=skillPlan.selected.filter(s=>s.permission==='ask').map(s=>s.name),browserTools=role==='visual-qa'&&methodologies.some(name=>['hi-browser-testing','hi-visual-qa','hi-accessibility-review'].includes(name))?[...HI_BROWSER_EXECUTION_TOOL_IDS]:[],askGatedPermissionKeys=Object.entries(surface.permissions.decisions).filter(([,value])=>value==='ask').map(([name])=>name),askGatedToolIds=new Set(askGatedPermissionKeys.flatMap(name=>name==='edit'?['edit','write','apply_patch']:name==='todowrite'?['todowrite','todoread']:[name])),taskTools=[...surface.tools.filter(t=>(t!=='skill'||methodologies.length>0)&&!askGatedToolIds.has(t)),...browserTools]
    const profile:ExecutionProfile={role,category,task:{objective,scope:[...scope],dependencies:[...dependencies],required_evidence:[...requiredEvidence]},tools:taskTools,model:selected.primary,model_variant:input.modelVariant??selected.primaryVariant,fallback_models:selected.fallbacks,fallback_variants:selected.fallbackVariants,fallback_reasons:selected.fallbackReasons,methodologies,permission_profile:{skill_tool_enabled:skillToolEnabled,skill_permissions:permissionMap??{},external_effects:'parent-only',recursive_task:'deny',native:surface.permissions},verification_policy:{...m.execution.verification_policy,requiredKinds:[...m.execution.verification_policy.requiredKinds]},max_context_chars:DEFAULT_CONTEXT_BUDGET.max_context_chars,max_handoff_chars:DEFAULT_CONTEXT_BUDGET.max_handoff_chars,max_result_chars:DEFAULT_CONTEXT_BUDGET.max_result_chars,max_artifacts:DEFAULT_CONTEXT_BUDGET.max_artifacts}
    const task=createTask(m,{objective,role,category,scope,constraints,dependencies,requiredEvidence,obligationIds,contextReferences:selectedContextReferences,executionProfile:profile});bindMethodologyNeeds(m,methodologies,{taskId:task.id,obligationIds:task.obligation_ids});if(isolationRequired){if(!this.workspaceRuntime){task.status='blocked';const marker=`workspace-executor-unavailable:${task.id}`;m.execution.blockers=[...new Set([...m.execution.blockers,marker])];throw new Error('Hi WorkspaceExecutor is unavailable for required isolation')}const decision=this.workspaceRuntime.decision(m,task,{required:true,reason:isolationReason});try{await this.workspaceRuntime.provision(m,task,decision)}catch(error){task.status='blocked';task.updated_at=Date.now();const marker=`workspace-provision-failed:${task.id}`;m.execution.blockers=[...new Set([...m.execution.blockers,marker])];appendLedger(m,'workspace.provision-failed',{task_id:task.id,payload:{error:String(error)}});throw error}}const provenance=methodologyProvenance(skillPlan.selected),worker=createWorker(m,task,selected.primary,selected.fallbacks,methodologies,provenance);worker.requested_model=input.model;worker.requested_model_variant=input.modelVariant;worker.model_selection_reason=[...selected.reason];worker.fallback_history=[]
    for(const ref of task.context_artifacts)if(ref.source_ref.startsWith('hi-artifact:'))contextArtifactStore.bindConsumer(ref.source_ref.slice('hi-artifact:'.length),task.id)
    const artifactContext=task.context_artifacts.map(a=>{const id=a.source_ref.startsWith('hi-artifact:')?a.source_ref.slice('hi-artifact:'.length):undefined,stored=id?contextArtifactStore.get(id):undefined;if(stored?.freshness==='FRESH')return `artifact:${stored.artifact_id}:${stored.summary}\n${clipText(stored.content,3000)}`;if(stored)return `artifact-stale:${stored.artifact_id}:${stored.summary}`;return `${a.kind}:${a.title??a.source_handle_id??a.id}${a.summary?` — ${a.summary}`:''}`}),verificationHint=targetedVerificationHint(this.projectRoot,task.scope.length?task.scope:(m.vcs.changed_files.length?m.vcs.changed_files:m.identity.intent.likelyTargets??[])),semanticContexts=semanticContextsForTargets(this.projectRoot,task.scope,task.id,3000),semanticContext=semanticContexts.map(renderSemanticContext),explicitRelevant=input.relevantContext??[],boundedRuntimeRelevant=[...(verificationHint?[verificationHint]:[]),...semanticContext,...artifactContext];if(semanticContexts.length)appendLedger(m,'context.semantic-selected',{task_id:task.id,payload:{items:semanticContexts.slice(0,6).map(x=>({id:x.id,source_ref:x.source_ref,source_hash:x.source_hash.slice(0,16),symbols:x.symbols.length,chars:x.budget.used_chars})),total_chars:semanticContexts.reduce((n,x)=>n+x.budget.used_chars,0)}});let nativeSummary:string|undefined,relevantForHandoff=[...explicitRelevant,...boundedRuntimeRelevant]
    if(relevantForHandoff.join('\n').length>profile.max_context_chars){if(this.childHost.capabilities.summarize)try{const summary=await this.childHost.summarize(m.identity.session_id);nativeSummary=clipText(typeof summary==='string'?summary:JSON.stringify(summary),Math.min(6000,Math.floor(profile.max_context_chars/2)));relevantForHandoff=[`native-session-summary:${nativeSummary}`,...boundedRuntimeRelevant];appendLedger(m,'context.native-summary-used',{task_id:task.id,payload:{source_session:m.identity.session_id,replaced_explicit_context:true}})}catch(error){appendLedger(m,'context.native-summary-unavailable',{task_id:task.id,payload:{error:String(error)}})};if(!nativeSummary)relevantForHandoff=clipList(relevantForHandoff,profile.max_context_chars)}
    const askGatedTools=[...askGatedPermissionKeys].sort(),askGatedInstruction=askGatedTools.length?`host ask-gated tools are intentionally unavailable on this child surface: ${askGatedTools.join(', ')}. Do not wait or retry for them. If one is essential to implementation, return BLOCKED/NEEDS_CONTEXT with the exact required action; otherwise finish the allowed implementation and let Hi keep parent/control-plane verification open.`:undefined
    const buildHandoff=()=>{const preexisting=Object.keys(worker.native_diff_baseline??{}).slice(0,60),core=workerHandoffText({objective,scope:task.scope,constraints:clipList([...(task.constraints??[]),'minimum sufficient change','no unrequested publish/push/deploy','return compact evidence',askGatedInstruction??'',preexisting.length?`pre-existing user dirty paths at worker start: ${preexisting.join(', ')}; preserve their exact baseline state unless the task explicitly requires changing them; never use git checkout/reset/restore in a way that discards user-owned edits`:'no pre-existing native dirty paths were observed at worker start',verificationEconomyInstruction(m)],5000),required_evidence:task.requiredEvidence,relevant_context:clipList(relevantForHandoff,profile.max_context_chars),methodologies:worker.selected_methodologies,methodology_exit_requirements:worker.selected_methodologies.flatMap(name=>{const item=catalog.find(x=>x.name===name);return item?[`${name}: ${item.exitRequirements.join(', ')}`]:[]}),approval_gated_methodologies:approvalGated,expected_output:{status:true,summary:true,changed_files:true,scope_expansions:true,evidence:true,findings:isHiReviewerRole(worker.role)?true:undefined,open_issues:true}},profile.max_handoff_chars),full=[ownershipContract('child',worker.selected_methodologies),core].filter(Boolean).join('\n\n');return clipText(full,profile.max_handoff_chars)}
    const chain=[selected.primary,...selected.fallbacks].filter((x):x is string=>Boolean(x)),toolOverrides=promptToolOverrides(profile.tools)
    const run=()=>this.registry.dedupeSpawn(worker.fingerprint,async()=>{let lastError:unknown=new Error('No runtime model available');for(let i=0;i<chain.length;i++){if(m.identity.status!=='active'||m.continuation.user_interrupted||worker.status==='cancelled'){worker.status='cancelled';task.status='cancelled';throw new Error('Mission stopped before worker dispatch')}const model=chain[i],variant=model===selected.primary?(input.modelVariant??selected.primaryVariant):selected.fallbackVariants[model],provider=providerOf(model),runtimeCandidate=runtimeModelCandidateStatus(model,this.getModels(),this.getConfig(),this.getHostConfig());if(!runtimeCandidate.ok){lastError=new Error(`Runtime model candidate rejected at dispatch: ${model}: ${runtimeCandidate.reason}`);appendLedger(m,'model.fallback.skipped',{task_id:task.id,worker_id:worker.id,payload:{model,reason:runtimeCandidate.reason,index:i,phase:'dispatch-revalidation'}});continue}const reservation=reserveTaskRuntimeDispatch(m,worker,model,this.scheduler);if(!reservation.accepted){lastError=new Error(`Worker scheduler admission unavailable: ${reservation.reason}`);appendLedger(m,'model.fallback.skipped',{task_id:task.id,worker_id:worker.id,payload:{model,reason:reservation.reason,index:i,source:'scheduler'}});continue}if(!this.scheduler.acquire(worker.id,provider,model==='host-default'?undefined:model)){releaseTaskRuntimeReservation(m,worker.id);lastError=new Error('Legacy resource tracker disagreed with scheduler admission');appendLedger(m,'scheduler.resource-tracker-mismatch',{task_id:task.id,worker_id:worker.id,payload:{model,index:i}});continue}worker.model=model;worker.model_variant=variant;try{worker.status='starting';task.status='queued';this.recordModelProjection(worker,model,variant);const spawned=await this.#child.createForTask(m.identity.session_id,`Hi · ${role} · ${objective.slice(0,60)}`,role,model==='host-default'?undefined:model,variant,input.forkFromSession,this.workspaceBinding(m,task.id)),child=spawned.child;if(input.forkFromSession)appendLedger(m,'worker.session-fork',{task_id:task.id,worker_id:worker.id,payload:{source_session:input.forkFromSession,native:spawned.fork.nativeAvailable,used:spawned.fork.used,reason:spawned.fork.reason}});if(m.identity.status!=='active'||m.continuation.user_interrupted||(worker as WorkerState).status==='cancelled'){if(child?.id)try{await this.abortNativeSession(m,child.id,'spawn-cancelled',worker.id,task.id)}catch{};throw new Error('Mission stopped during worker spawn')}worker.session_id=child?.id;if(!worker.session_id)throw new Error('Child session id missing');const bound=bindTaskRuntimeHost(m,worker.id,worker.session_id);if(!bound.accepted)throw new Error(`Scheduler host binding failed: ${bound.reason}`);recordPreexistingUserBaseline(m,await this.captureNativeDiff(worker,'baseline'));worker.generation_at_spawn=m.continuation.generation;worker.status='busy';worker.started_at=Date.now();task.status='running';this.registry.set(worker);if(i>0){const fallbackReason=selected.fallbackReasons[i-1]?.reason??`fallback-index:${i}`;worker.fallback_history=[...(worker.fallback_history??[]),{from:chain[i-1],to:model,variant,reason:fallbackReason,phase:'dispatch',at:Date.now()}]}void this.events?.(runtimeSignal('worker.started',m.identity.mission_id,{task_id:task.id,worker_id:worker.id,payload:{model,variant,role}}));appendLedger(m,'worker.started',{task_id:task.id,worker_id:worker.id,payload:{session_id:worker.session_id,model,variant,index:i,reason:i===0?(input.modelVariant?[...selected.reason,'user-specified-variant']:selected.reason):[selected.fallbackReasons[i-1]?.reason??'runtime fallback',`fallback-index:${i}`]}});const handoff=buildHandoff();appendLedger(m,'worker.handoff',{task_id:task.id,worker_id:worker.id,payload:{chars:handoff.length,methodologies:worker.selected_methodologies.length,tools:profile.tools.slice(0,20),permission_source:profile.permission_profile.native?.source,context_budget:profile.max_context_chars,handoff_budget:profile.max_handoff_chars,result_budget:profile.max_result_chars}});beginWorkerAttempt(task,worker);await this.sendProviderPrompt(worker.session_id,handoff,role,model==='host-default'?undefined:model,variant,toolOverrides);return worker}catch(error){lastError=error;let hostStopped=true;if(worker.session_id){try{hostStopped=await this.abortNativeSession(m,worker.session_id,'dispatch-fallback',worker.id,task.id)}catch{hostStopped=false}if(hostStopped)worker.session_id=undefined}if(!hostStopped){appendLedger(m,'worker.start.abort-blocked',{task_id:task.id,worker_id:worker.id,payload:{model,index:i,error:String(error)}});throw new Error(`Scheduler reservation retained because host abort could not be verified for worker ${worker.id}`)}this.scheduler.release(worker.id);releaseTaskRuntimeReservation(m,worker.id);if(m.identity.status!=='active'||m.continuation.user_interrupted||(worker as WorkerState).status==='cancelled'){worker.status='cancelled';task.status='cancelled';appendLedger(m,'worker.start.cancelled',{task_id:task.id,worker_id:worker.id,payload:{model,index:i,error:String(error)}});throw error}appendLedger(m,'model.fallback.failed',{task_id:task.id,worker_id:worker.id,payload:{model,index:i,error:String(error)}});worker.status='created';task.status='created'}}worker.status='failed';const liveStatuses=chain.map(model=>({model,...runtimeModelCandidateStatus(model,this.getModels(),this.getConfig(),this.getHostConfig())}));const policyUnavailable=liveStatuses.length>0&&liveStatuses.every(x=>!x.ok);if(policyUnavailable){task.status='blocked';const marker=`model-dispatch-unavailable:${task.id}`;m.execution.blockers=[...new Set([...m.execution.blockers,marker])];task.result={status:'BLOCKED',summary:'No selected role model/fallback remains runtime-available and policy-permitted at dispatch time.',changed_files:[],evidence:[],open_issues:[marker],needs_context:['refresh provider/model inventory or routing/provider permissions']};appendLedger(m,'worker.start.model-unavailable',{task_id:task.id,worker_id:worker.id,payload:{attempted:liveStatuses}})}else task.status='failed';await this.cleanupWorkspaceForTask(m,task.id);appendLedger(m,'worker.start.failed',{task_id:task.id,worker_id:worker.id,payload:{error:String(lastError),attempted_models:chain}});throw lastError})
    syncMissionGates(m);if(!this.admittedModel(m,worker,chain)){this.queueTask(m,worker,run);return{task_id:task.id,worker_id:worker.id,model:worker.model,methodologies:worker.selected_methodologies,selection_reason:[...routed.reason,...selected.reason,...selected.fallbackReasons.map(x=>`${x.model}:${x.reason}`),'queued:runtime-capacity-or-prerequisite',...skillPlan.reason],readiness:'WAIT',preconditions:preflight.items}}
    const spawned=await run();if(spawned.id!==worker.id){const duplicateTask=m.execution.tasks.find(t=>t.id===task.id);if(duplicateTask&&['created','queued'].includes(duplicateTask.status))m.execution.tasks=m.execution.tasks.filter(t=>t.id!==duplicateTask.id);m.execution.workers=m.execution.workers.filter(w=>w.id!==worker.id);await this.cleanupWorkspaceForTask(m,task.id);appendLedger(m,'worker.spawn.deduped',{worker_id:spawned.id,payload:{discarded_worker_id:worker.id,fingerprint:worker.fingerprint}});const spawnedTask=m.execution.tasks.find(t=>t.id===spawned.task_id);return{task_id:spawnedTask?.id??spawned.task_id,worker_id:spawned.id,session_id:spawned.session_id,model:spawned.model,methodologies:spawned.selected_methodologies,selection_reason:[...routed.reason,...selected.reason,'deduped:existing-spawn',...selected.fallbackReasons.map(x=>`${x.model}:${x.reason}`),...skillPlan.reason],readiness:'READY',preconditions:preflight.items}}return{task_id:task.id,worker_id:spawned.id,session_id:spawned.session_id,model:spawned.model,methodologies:spawned.selected_methodologies,selection_reason:[...routed.reason,...selected.reason,...selected.fallbackReasons.map(x=>`${x.model}:${x.reason}`),...skillPlan.reason],readiness:'READY',preconditions:preflight.items}
  }
  async resume(m:MissionState,taskID:string):Promise<{task_id:string;worker_id:string;session_id?:string;model?:string;methodologies:string[];selection_reason:string[];readiness:'READY'|'WAIT';preconditions:Array<{id:string;decision:string;reason:string}>}>{
    const task=m.execution.tasks.find(t=>t.id===taskID);if(!task)throw new Error(`Unknown Hi task ${taskID}`)
    const worker=m.execution.workers.find(w=>w.id===task.worker_id||w.task_id===task.id);if(!worker)throw new Error(`Hi task ${taskID} has no worker`)
    if(!worker.session_id)throw new Error(`Hi task ${taskID} has no reusable child session`)
    if(worker.status!=='ready'||!task.result||!['FIX_REQUIRED','NEEDS_CONTEXT','BLOCKED'].includes(task.result.status))throw new Error(`Hi task ${taskID} is not resumable from status ${task.status}/${task.result?.status??'none'}`)
    return this.start(m,{objective:task.objective,role:task.role,category:task.category,scope:[...task.scope],dependencies:[...task.dependencies],requiredEvidence:[...task.requiredEvidence],obligationIds:[...task.obligation_ids],model:worker.model,modelVariant:worker.model_variant,constraints:[...task.constraints],resumeTaskId:task.id})
  }
  async pauseForSemanticAssessment(m:MissionState):Promise<number>{
    if(m.identity.semantic_assessment.status!=='pending')return 0
    let paused=0
    for(const worker of m.execution.workers.filter(w=>['created','queued','starting','ready','busy'].includes(w.status))){
      const task=m.execution.tasks.find(t=>t.id===worker.task_id);if(!task)continue
      worker.semantic_pause_revision=m.identity.semantic_assessment.revision
      if(!worker.session_id){worker.status='cancelled';task.status='cancelled';this.scheduler.release(worker.id);releaseTaskRuntimeReservation(m,worker.id,'CANCEL');this.registry.delete(worker.id);this.#queue=this.#queue.filter(q=>q.worker.id!==worker.id);await this.cleanupWorkspaceForTask(m,task.id);appendLedger(m,'worker.semantic-cancelled-before-start',{task_id:task.id,worker_id:worker.id,payload:{revision:m.identity.semantic_assessment.revision}});paused++;continue}
      if(['starting','busy'].includes(worker.status)){const stopped=await this.abortNativeSession(m,worker.session_id,'semantic-quarantine',worker.id,task.id);if(!stopped){const marker=`semantic-abort-unavailable:${task.id}:${worker.id}`;m.execution.blockers=[...new Set([...m.execution.blockers,marker])];appendLedger(m,'worker.semantic-pause-blocked',{task_id:task.id,worker_id:worker.id,payload:{revision:m.identity.semantic_assessment.revision,reason:'abort-unavailable'}});continue}this.scheduler.release(worker.id);releaseTaskRuntimeReservation(m,worker.id)}
      worker.status='ready';task.status=task.result?'waiting':'waiting';this.registry.set(worker);appendLedger(m,'worker.semantic-paused',{task_id:task.id,worker_id:worker.id,payload:{revision:m.identity.semantic_assessment.revision,session_id:worker.session_id}});paused++
    }
    this.#queue=this.#queue.filter(q=>q.mission.identity.mission_id!==m.identity.mission_id||q.worker.status!=='cancelled');syncMissionGates(m);return paused
  }
  async resumeAfterSemanticAssessment(m:MissionState,messageKind:string):Promise<number>{
    if(m.identity.semantic_assessment.status!=='assessed'||m.identity.status!=='active')return 0
    let resumed=0
    for(const worker of m.execution.workers.filter(w=>w.semantic_pause_revision===m.identity.semantic_assessment.revision&&w.status==='ready'&&Boolean(w.session_id))){
      const task=m.execution.tasks.find(t=>t.id===worker.task_id);if(!task||!worker.session_id)continue
      try{this.workspaceBinding(m,task.id)}catch(error){const marker=`workspace-orphan:${task.id}`;m.execution.blockers=[...new Set([...m.execution.blockers,marker])];appendLedger(m,'worker.semantic-resume-blocked',{task_id:task.id,worker_id:worker.id,payload:{reason:String(error)}});continue}
      const model=worker.model,resumeAdmission=this.reserveExistingSessionAttempt(m,worker,model)
      if(!resumeAdmission.ok){appendLedger(m,'worker.semantic-resume-deferred',{task_id:task.id,worker_id:worker.id,payload:{revision:m.identity.semantic_assessment.revision,reason:resumeAdmission.reason}});continue}
      worker.generation_at_spawn=m.continuation.generation;worker.parent_mission_id=m.identity.mission_id;worker.status='busy';worker.semantic_pause_revision=undefined;worker.started_at=Date.now();task.status='running';this.registry.set(worker)
      beginWorkerAttempt(task,worker);this.recordModelProjection(worker,model,worker.model_variant);await this.sendProviderPrompt(worker.session_id,clipText([`Hi semantic follow-up reconciliation for existing task ${task.id}.`,`Follow-up kind: ${messageKind}.`,`Current mission objective: ${m.identity.objective}`,`Current user constraints: ${(task.constraints??[]).join(' | ')||'none'}.`,`Still-selected methodologies: ${worker.selected_methodologies.join(', ')||'none'}.`,'Continue the SAME task/session from current context. Preserve completed work and evidence. Do not restart planning. If the follow-up creates separate work outside this task, report it rather than silently expanding scope. Return the normal structured WorkerResult.'].join('\n'),DEFAULT_CONTEXT_BUDGET.max_handoff_chars),worker.role,model==='host-default'?undefined:model,worker.model_variant,promptToolOverrides(task.execution_profile?.tools??[]));appendLedger(m,'worker.semantic-resumed',{task_id:task.id,worker_id:worker.id,payload:{revision:m.identity.semantic_assessment.revision,message_kind:messageKind,session_id:worker.session_id}});resumed++
    }
    return resumed
  }
  async reconcileUserConstraint(m:MissionState,text:string):Promise<number>{
    if(m.identity.status!=='active'||m.continuation.user_interrupted)return 0
    let reconciled=0
    for(const worker of m.execution.workers.filter(w=>['created','queued','starting','busy','ready'].includes(w.status))){
      const task=m.execution.tasks.find(t=>t.id===worker.task_id);if(!task)continue
      try{this.workspaceBinding(m,task.id)}catch(error){const marker=`workspace-orphan:${task.id}`;m.execution.blockers=[...new Set([...m.execution.blockers,marker])];appendLedger(m,'worker.constraint-rebase.blocked',{task_id:task.id,worker_id:worker.id,payload:{reason:String(error)}});continue}
      const beforeMethodologies=[...worker.selected_methodologies]
      const stillRequired=(name:string)=>m.methodology.methodology_needs.some(need=>need.name===name&&(!need.task_id||need.task_id===task.id))
      worker.selected_methodologies=worker.selected_methodologies.filter(stillRequired)
      worker.methodologies=worker.methodologies.filter(item=>worker.selected_methodologies.includes(item.name))
      if(task.execution_profile)task.execution_profile.methodologies=[...worker.selected_methodologies]
      const suppressedMethodologies=beforeMethodologies.filter(name=>!worker.selected_methodologies.includes(name))
      if(suppressedMethodologies.length)appendLedger(m,'worker.methodology-suppressed',{task_id:task.id,worker_id:worker.id,payload:{methodologies:suppressedMethodologies,generation:m.continuation.generation,reason:'user-constraint-superseded-intent'}})
      task.constraints??=[];if(!task.constraints.includes(text))task.constraints.push(text);task.updated_at=Date.now()
      // Queued/not-running work will build its handoff lazily from task.constraints.
      if(!worker.session_id||['created','queued'].includes(worker.status)||(worker.status==='ready'&&!worker.semantic_pause_revision)){worker.generation_at_spawn=m.continuation.generation;worker.parent_mission_id=m.identity.mission_id;appendLedger(m,'worker.constraint-updated',{task_id:task.id,worker_id:worker.id,payload:{mode:'deferred',generation:m.continuation.generation,constraint:text.slice(0,300)}});reconciled++;continue}
      const oldSession=worker.session_id,model=worker.model,variant=worker.model_variant
      // A restrictive user constraint invalidates the in-flight prompt. Abort and rebase the SAME
      // task/worker identity onto a fresh child session so late idle/result callbacks from the old
      // session cannot be mistaken for work performed under the new constraint.
      this.registry.delete(worker.id)
      try{const stopped=await this.abortNativeSession(m,oldSession,'constraint-rebase',worker.id,task.id);if(!stopped)throw new Error('OpenCode session abort unavailable for constraint rebase')}catch(error){const marker=`constraint-abort-unavailable:${task.id}:${worker.id}`;m.execution.blockers=[...new Set([...m.execution.blockers,marker])];worker.status='busy';task.status='running';appendLedger(m,'worker.constraint-rebase.blocked',{task_id:task.id,worker_id:worker.id,payload:{from_session:oldSession,error:String(error),generation:m.continuation.generation}});this.registry.set(worker);continue}
      this.scheduler.release(worker.id);releaseTaskRuntimeReservation(m,worker.id);worker.session_id=undefined;worker.status='ready';task.status='waiting';this.registry.set(worker)
      if(!model){appendLedger(m,'worker.constraint-rebase.deferred',{task_id:task.id,worker_id:worker.id,payload:{reason:'model-missing'}});continue}
      const reservation=reserveTaskRuntimeDispatch(m,worker,model,this.scheduler);if(!reservation.accepted){appendLedger(m,'worker.constraint-rebase.deferred',{task_id:task.id,worker_id:worker.id,payload:{reason:reservation.reason}});continue}
      if(!this.scheduler.acquire(worker.id,providerOf(model),model==='host-default'?undefined:model)){releaseTaskRuntimeReservation(m,worker.id);appendLedger(m,'worker.constraint-rebase.deferred',{task_id:task.id,worker_id:worker.id,payload:{reason:'legacy-resource-backstop'}});continue}
      try{
        this.recordModelProjection(worker,model,variant);const child=await this.#child.create(m.identity.session_id,`Hi · ${worker.role} · constraint update · ${task.objective.slice(0,45)}`,worker.role,model==='host-default'?undefined:model,variant,this.workspaceBinding(m,task.id))
        if(!child?.id)throw new Error('Constraint rebase child session id missing')
        const recoverySessionID=String(child.id);worker.session_id=recoverySessionID;const bound=bindTaskRuntimeHost(m,worker.id,recoverySessionID);if(!bound.accepted)throw new Error(`Scheduler host binding failed during constraint rebase: ${bound.reason}`);worker.loaded_methodologies=[];worker.semantic_pause_revision=undefined;recordPreexistingUserBaseline(m,await this.captureNativeDiff(worker,'baseline'));worker.parent_mission_id=m.identity.mission_id;worker.generation_at_spawn=m.continuation.generation;worker.status='busy';worker.started_at=Date.now();task.status='running';this.registry.set(worker)
        const constraintExit=worker.selected_methodologies.flatMap(name=>{const item=methodologyCatalog(this.projectRoot).find(x=>x.name===name);return item?[`${name}: ${item.exitRequirements.join(', ')}`]:[]});const handoff=clipText([ownershipContract('child',worker.selected_methodologies),`Hi USER CONSTRAINT UPDATE for existing task ${task.id}.`,`OBJECTIVE: ${task.objective}`,`SCOPE: ${task.scope.join(', ')||'bounded by objective'}`,`CURRENT USER CONSTRAINTS: ${task.constraints.join(' | ')}`,`OBSERVED CHANGED FILES SO FAR: ${m.vcs.changed_files.slice(-30).join(', ')||'none'}`,`METHODOLOGY EXIT REQUIREMENTS: ${constraintExit.join(' | ')||'none'}`,worker.selected_methodologies.length?'This is a fresh child session. Reload every still-selected methodology through the native skill tool before applying it.':'No methodology remains selected after the user constraint.','The previous child session was aborted because the user changed constraints. The latest constraint supersedes conflicting prior instructions. Do not write to prohibited surfaces. If prohibited files were already changed, report that explicitly; do not conceal or assume those edits are acceptable. Reconcile the existing task under the new constraint with the minimum safe change.','Return the normal structured WorkerResult.'].join('\n'),DEFAULT_CONTEXT_BUDGET.max_handoff_chars)
        beginWorkerAttempt(task,worker);await this.sendProviderPrompt(child.id,handoff,worker.role,model==='host-default'?undefined:model,variant,promptToolOverrides(task.execution_profile?.tools??[]))
        appendLedger(m,'worker.constraint-rebased',{task_id:task.id,worker_id:worker.id,payload:{from_session:oldSession,to_session:worker.session_id,generation:m.continuation.generation,constraint:text.slice(0,300)}});void this.events?.(runtimeSignal('worker.constraint-rebased',m.identity.mission_id,{task_id:task.id,worker_id:worker.id,payload:{generation:m.continuation.generation}}));reconciled++
      }catch(error){let stopped=true;if(worker.session_id)try{stopped=await this.abortNativeSession(m,worker.session_id,'constraint-rebase-failed',worker.id,task.id)}catch{stopped=false};if(stopped){this.scheduler.release(worker.id);releaseTaskRuntimeReservation(m,worker.id);worker.session_id=undefined;worker.status='ready';task.status=task.result?'waiting':'blocked'}else{const marker=`constraint-rebase-recovery-abort-unavailable:${task.id}:${worker.id}`;m.execution.blockers=[...new Set([...m.execution.blockers,marker])];worker.status='busy';task.status='running'}this.registry.set(worker);appendLedger(m,'worker.constraint-rebase.failed',{task_id:task.id,worker_id:worker.id,payload:{from_session:oldSession,error:String(error),generation:m.continuation.generation,host_stopped:stopped}})}
    }
    syncMissionGates(m);return reconciled
  }
  async noteNativeWriteSet(m:MissionState,workerID:string,files:string[],source='session-diff',stateHash?:string):Promise<void>{return this.#results.noteNativeWriteSet(m,workerID,files,source,stateHash)}
  noteNativeStatus(m:MissionState,workerID:string,status:string):void{this.#results.noteNativeStatus(m,workerID,status)}
  applyResult(m:MissionState,workerID:string,result:WorkerResult):void{this.#results.applyResult(m,workerID,result)}
  async recoverStagnation(m:MissionState,level:number):Promise<boolean>{return this.#recovery.recoverStagnation(m,level)}
  async recoverRuntimeFailure(m:MissionState,workerID:string,error:string):Promise<boolean>{return this.#recovery.recoverRuntimeFailure(m,workerID,error)}
  fail(m:MissionState,workerID:string,error:string):void{this.#recovery.fail(m,workerID,error)}
  peek(m:MissionState,id:string):any{const task=m.execution.tasks.find(t=>t.id===id),worker=m.execution.workers.find(w=>w.id===id||w.id===task?.worker_id);return{task,worker}}
  async awaitTask(m:MissionState,id:string,timeoutMs=30_000):Promise<any>{let current=this.peek(m,id),status=current?.task?.status??current?.worker?.status??'unknown';if(['completed','failed','cancelled','blocked'].includes(status))return{...current,status,terminal:true,changed:false,timed_out:false};const worker=current?.worker;if(!worker)return{...current,status,terminal:false,changed:false,timed_out:false};const changed=await this.registry.waitForChange(worker.id,timeoutMs);current=this.peek(m,id);status=current?.task?.status??current?.worker?.status??'unknown';const terminal=['completed','failed','cancelled','blocked'].includes(status);return{...current,status,terminal,changed,timed_out:!changed&&!terminal}}
  list(m:MissionState):any[]{return m.execution.tasks.map(t=>({task:t,worker:m.execution.workers.find(w=>w.id===t.worker_id)}))}
  async cancelAll(m:MissionState):Promise<number>{let n=0;for(const w of [...m.execution.workers])if(['created','queued','starting','ready','busy'].includes(w.status))if(await this.cancel(m,w.id))n++;return n}
  async cancel(m:MissionState,id:string):Promise<boolean>{const task=m.execution.tasks.find(t=>t.id===id),worker=m.execution.workers.find(w=>w.id===id||w.id===task?.worker_id);if(!worker)return false;if(worker.session_id){const stopped=await this.abortNativeSession(m,worker.session_id,'worker-cancel',worker.id,worker.task_id);if(!stopped){appendLedger(m,'worker.cancel.blocked',{task_id:worker.task_id,worker_id:worker.id,payload:{reason:'abort-unavailable'}});return false}}const reservationRelease=releaseTaskRuntimeReservation(m,worker.id,'CANCEL');if(!reservationRelease.accepted){appendLedger(m,'worker.cancel.scheduler-blocked',{task_id:worker.task_id,worker_id:worker.id,payload:{reason:reservationRelease.reason}});return false}worker.status='cancelled';this.scheduler.release(worker.id);const t=m.execution.tasks.find(x=>x.id===worker.task_id);if(t)t.status='cancelled';this.registry.delete(worker.id);this.#queue=this.#queue.filter(q=>q.worker.id!==worker.id);await this.cleanupWorkspaceForTask(m,worker.task_id);appendLedger(m,'worker.cancelled',{task_id:t?.id,worker_id:worker.id});syncMissionGates(m);this.drainQueue();return true}
}
