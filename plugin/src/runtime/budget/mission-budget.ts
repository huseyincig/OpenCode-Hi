export type FailureClass='code-failure'|'test-failure'|'environment-failure'|'provider-failure'|'permission-failure'|'authority-failure'|'tool-failure'|'context-insufficiency'|'model-capability-insufficiency'|'contract-ambiguity'
export interface MissionBudget{maxTurns:number;maxModelCalls:number;maxToolCalls:number;maxDelegations:number;maxParallelism:number;maxSameFailureRetries:number;maxContextChars:number;planningBudget:number;verificationBudget:number;reviewBudget:number}
export const DEFAULT_MISSION_BUDGET:MissionBudget={maxTurns:20,maxModelCalls:12,maxToolCalls:80,maxDelegations:4,maxParallelism:2,maxSameFailureRetries:2,maxContextChars:120000,planningBudget:1,verificationBudget:4,reviewBudget:1}
export function materiallyDifferentRetry(previous:{failure:FailureClass;strategy:string;evidence?:string;tool?:string;model?:string},next:{failure:FailureClass;strategy:string;evidence?:string;tool?:string;model?:string}):boolean{return previous.failure!==next.failure||previous.strategy!==next.strategy||previous.evidence!==next.evidence||previous.tool!==next.tool||previous.model!==next.model}
