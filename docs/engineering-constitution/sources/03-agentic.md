# agentic

Status: SOURCE VERSION MISMATCH — NEEDS ARCHIVE RESOLUTION

## Identity

- Public repository resolved: `transitive-bullshit/agentic` (archived).
- Reference action: ADAPT.
- The path cited by the supplied Source Reuse Matrix (`src/cli/metadata.ts`) is not present at that path in the current public `main` tree.

## Source surfaces inspected

- current public repository root and structure.
- attempted exact matrix path and code search; no verified match yet.

## Verified source facts

- The public repository is a large archived monorepo with apps/docs/examples/legacy areas.
- The exact supplied artifact/document metadata source has not yet been tied to a public commit.

## Useful engineering patterns

PENDING until the supplied archive/version is identified. Do not infer the intended metadata lifecycle from the current public tree.

## Foreign / accidental semantics to reject

- Do not silently substitute a different version/path and claim source equivalence.

## Hi mapping

- Artifact lifecycle remains grounded in current Hi implementation and other verified references until this source version is resolved.

## Open questions

- Locate the originally supplied archive or commit that contains `src/cli/metadata.ts` before using this source for architectural decisions.
